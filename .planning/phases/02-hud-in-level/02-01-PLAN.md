---
phase: 02-hud-in-level
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs
autonomous: true
requirements:
  - HUD-02
must_haves:
  truths:
    - "TMP Essential Resources importados (LiberationSans SDF visible en Assets/TextMesh Pro/Resources/)"
    - "PlayerMoneyStore.OnMoneyChanged dispara al llamar Add, Spend y Clear con el nuevo saldo"
  artifacts:
    - path: "Assets/TextMesh Pro/Resources/Fonts & Materials/LiberationSans SDF.asset"
      provides: "Font asset TMP requerido para TextMeshProUGUI"
    - path: "Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs"
      provides: "Evento estático OnMoneyChanged agregado"
      contains: "event Action<int> OnMoneyChanged"
  key_links:
    - from: "PlayerMoneyStore.Add/Spend/Clear"
      to: "HUDController (Plan 02)"
      via: "OnMoneyChanged?.Invoke(Money)"
      pattern: "OnMoneyChanged\\?\\."
---

<objective>
Importar los TMP Essential Resources (bloqueante para cualquier TextMeshProUGUI) y agregar el evento estático OnMoneyChanged a PlayerMoneyStore (D-03).

Purpose: Destraba los dos pre-requisitos del HUD — el font asset de TMP y el canal de notificaciones de dinero. Sin estos dos ítems, el Plan 02 no puede crear el Canvas con texto ni recibir cambios de saldo.

Output: TMP Essentials importados en disco; PlayerMoneyStore con evento OnMoneyChanged que dispara Add, Spend y Clear.
</objective>

<execution_context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/02-hud-in-level/02-CONTEXT.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/02-hud-in-level/02-RESEARCH.md
</execution_context>

<context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/PROJECT.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/ROADMAP.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/02-hud-in-level/02-VALIDATION.md

<interfaces>
<!-- Fuentes de verdad que el ejecutor debe leer antes de modificar -->

De Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs (estado ACTUAL — sin OnMoneyChanged):
```csharp
public static class PlayerMoneyStore
{
    public static int Money { get; private set; } = 0;
    public static void Add(int amount) { ... Debug.Log($"[PlayerMoneyStore] +${amount}. Saldo: ${Money}"); }
    public static bool Spend(int amount) { ... Debug.Log($"[PlayerMoneyStore] -${amount}. Saldo: ${Money}"); return true/false; }
    public static bool CanAfford(int amount) => Money >= amount;
    public static void Clear() { Money = 0; }
}
```

Resultado esperado DESPUÉS de esta tarea (agrega event + invocaciones):
- `public static event Action<int> OnMoneyChanged;` declarado a nivel de clase
- `Add` llama `OnMoneyChanged?.Invoke(Money)` DESPUÉS de sumar
- `Spend` llama `OnMoneyChanged?.Invoke(Money)` DESPUÉS de restar (solo si el gasto fue exitoso)
- `Clear` llama `OnMoneyChanged?.Invoke(Money)` DESPUÉS de resetear a 0
- `using System;` debe estar presente (requerido para `Action<int>`)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Importar TMP Essential Resources</name>
  <what-built>El Unity MCP puede ejecutar este menu item automáticamente via execute_menu_item. Si el MCP no está conectado, hacerlo manualmente.</what-built>
  <how-to-verify>
    1. En el Unity Editor, ejecutar: Window > TextMeshPro > Import TMP Essential Resources
    2. En el diálogo que aparece, confirmar "Import".
    3. Esperar que Unity termine de importar (barra de progreso).
    4. Verificar que Assets/TextMesh Pro/Resources/ contiene "Fonts & Materials/LiberationSans SDF.asset" y "TMP Settings.asset".
    Via MCP: llamar execute_menu_item con "Window/TextMeshPro/Import TMP Essential Resources", luego read_console para confirmar sin errores.
  </how-to-verify>
  <resume-signal>Escribe "importado" cuando Assets/TextMesh Pro/Resources/Fonts &amp; Materials/LiberationSans SDF.asset existe en disco.</resume-signal>

  <read_first>
    - Assets/TextMesh Pro/ (verificar que la carpeta está vacía antes de importar)
    - .planning/phases/02-hud-in-level/02-RESEARCH.md §"Pitfall 1: TMP Essentials no importados"
  </read_first>

  <acceptance_criteria>
    - Assets/TextMesh Pro/Resources/Fonts &amp; Materials/LiberationSans SDF.asset existe en disco
    - Assets/TextMesh Pro/Resources/TMP Settings.asset existe en disco
    - Console del Editor sin errores de TMP tras la importación
    - Unity no emite "TMP Essential Resources not found" al crear un TextMeshProUGUI por código
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Agregar OnMoneyChanged a PlayerMoneyStore (D-03)</name>
  <files>Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs</files>

  <read_first>
    - Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs (leer completo antes de editar)
    - Assets/_Project/Core/Managers/DrunkManager.cs (ver patrón de event Action&lt;int&gt; OnAlcoholLevelChanged — análogo exacto)
    - .planning/phases/02-hud-in-level/02-RESEARCH.md §"Pattern 3: Evento OnMoneyChanged en PlayerMoneyStore"
  </read_first>

  <action>
    Modificar Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs para agregar el evento OnMoneyChanged (D-03). Cambios concretos:

    1. Agregar `using System;` al bloque de usings si no está presente (necesario para Action&lt;int&gt;).
    2. Declarar el evento estático justo antes de la propiedad Money:
       `public static event Action&lt;int&gt; OnMoneyChanged;`
       Con un summary XML en español: "Se dispara cuando el saldo cambia. Argumento: nuevo saldo."
    3. En Add: después de `Money += amount;` y DESPUÉS del Debug.Log existente, agregar `OnMoneyChanged?.Invoke(Money);`
    4. En Spend: después de `Money -= amount;` y DESPUÉS del Debug.Log existente, agregar `OnMoneyChanged?.Invoke(Money);` — SOLO dentro del bloque que confirma que el gasto fue exitoso (no antes del `if (Money &lt; amount) return false;`).
    5. En Clear: después de `Money = 0;`, agregar `OnMoneyChanged?.Invoke(Money);`

    Convenciones: namespace global (sin `namespace`), comentarios en español, no mover ni reformatear el código existente innecesariamente. El evento es estático porque PlayerMoneyStore es una clase estática.

    No agregar nada más — no crear HUDController ni ningún otro archivo en este plan.
  </action>

  <verify>
    Via MCP read_console: Console del Unity Editor sin errores de compilación tras guardar el archivo.
    Grep de validación: `grep -n "OnMoneyChanged" Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs` debe retornar al menos 4 líneas (declaración + 3 invocaciones).
  </verify>

  <acceptance_criteria>
    - `grep -c "OnMoneyChanged" Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs` retorna 4 o más (declaración + Invoke en Add, Spend, Clear)
    - `grep "using System" Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs` retorna una línea
    - `grep "event Action" Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs` retorna la declaración del evento
    - Console del Unity Editor compila sin errores (verificar con MCP read_console o abrir el Editor)
    - Los métodos Add, Spend y Clear NO perdieron su lógica original (Debug.Log, guards, retorno de Spend)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Juego single-player local, sin red, sin auth, sin input externo al Editor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | PlayerMoneyStore.OnMoneyChanged (evento estático) | accept | El evento expone el saldo a cualquier suscriptor del mismo dominio — aceptable en juego single-player sin red; no hay PII ni valor real |
| T-02-02 | Tampering | PlayerMoneyStore.Add/Spend/Clear con invocación de evento | accept | El juego es single-player; no hay superficie de ataque externa que pueda llamar estos métodos. Null-safe invoke con `?.` previene NPE |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Esta fase no instala packages externos — todo es Unity nativo (com.unity.ugui 2.0.0 ya en manifest). No hay gate de slopcheck aplicable |

**Conclusión:** Superficie de amenaza mínima. Un juego 3D de un solo jugador sin red, sin autenticación, sin parsing de input externo y sin packages de terceros nuevos. Los únicos riesgos son errores de programación (null refs), no amenazas de seguridad.
</threat_model>

<verification>
Tras completar ambas tareas de este plan:

1. `find "Assets/TextMesh Pro/Resources" -name "LiberationSans SDF.asset"` retorna un resultado
2. `grep -c "OnMoneyChanged" Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs` retorna >= 4
3. Console del Editor limpia (MCP read_console o compilación manual en el Editor)

El HUD no es visible todavía — eso llega en Plan 02. Este plan solo prepara los pre-requisitos.
</verification>

<success_criteria>
- TMP Essential Resources importados y confirmados en Assets/TextMesh Pro/Resources/
- PlayerMoneyStore.OnMoneyChanged declarado y disparando en Add, Spend y Clear
- El Unity Editor compila sin errores
- Los consumidores existentes de PlayerMoneyStore (SellCounter, PlayerPickup) no se rompen — sus llamadas a Add/Spend/Clear siguen funcionando igual
</success_criteria>

<output>
Crear .planning/phases/02-hud-in-level/02-01-SUMMARY.md al terminar este plan.
</output>
