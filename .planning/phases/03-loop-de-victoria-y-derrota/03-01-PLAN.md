---
phase: 03-loop-de-victoria-y-derrota
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/Gameplay/Systems/LethalObstacle.cs
  - Assets/_Project/Core/SceneManagement/GameResultStore.cs
  - Assets/_Project/Core/SceneManagement/HomeObjectsTotalStore.cs
  - Assets/_Project/Core/SceneManagement/DeliveredObjectsStore.cs
  - Assets/_Project/UI/HUD/HUDController.cs
autonomous: false
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04

must_haves:
  truths:
    - "El componente LethalObstacle existe, compila, y puede agregarse a cualquier GameObject en el Editor"
    - "GameResultStore expone GameResult.None/Victory/Defeat con Set() y Clear() (D-12)"
    - "HomeObjectsTotalStore expone Total con Set(int) y Clear()"
    - "DeliveredObjectsStore expone TakenCount (propiedad count de takenIds) (D-06)"
    - "HUDController.SetVisible(bool) oculta o muestra el Canvas del HUD sin errores"
    - "La escena Result.unity existe en Assets/Scenes/ y está registrada en Build Settings (D-07)"
  artifacts:
    - path: "Assets/_Project/Gameplay/Systems/LethalObstacle.cs"
      provides: "Componente marcador con enum ObstacleCategory (Casa, Arbol, Nino, Mascota)"
    - path: "Assets/_Project/Core/SceneManagement/GameResultStore.cs"
      provides: "Store estático con enum GameResult (None, Victory, Defeat)"
    - path: "Assets/_Project/Core/SceneManagement/HomeObjectsTotalStore.cs"
      provides: "Store estático con Total (int) para contar objetos de Home"
    - path: "Assets/Scenes/Result.unity"
      provides: "Escena dedicada de resultado registrada en Build Settings"
  key_links:
    - from: "GameResultStore"
      to: "ResultScreenController (plan 03-04)"
      via: "GameResultStore.Result leído en Start()"
    - from: "HomeObjectsTotalStore"
      to: "GameManager (plan 03-02)"
      via: "HomeObjectsTotalStore.Total leído en OnPlayerArrivedHome()"
    - from: "HUDController.SetVisible"
      to: "ResultScreenController (plan 03-04)"
      via: "llamado en Start() de la escena Result"
---

<objective>
Crear los contratos base (stores, componente marcador, métodos de visibilidad) y la
escena Result que todos los planes siguientes consumen. Es el bloque fundacional del
loop de victoria y derrota.

Purpose: Sin estos contratos los planes 03-02..04 no pueden compilar. La escena
Result.unity debe existir antes de que cualquier script llame LoadSceneAsync("Result").

Output:
- LethalObstacle.cs — componente marcador con enum ObstacleCategory
- GameResultStore.cs — store estático con enum GameResult
- HomeObjectsTotalStore.cs — store estático con int Total
- DeliveredObjectsStore.cs modificado — agrega TakenCount
- HUDController.cs modificado — agrega SetVisible(bool)
- Assets/Scenes/Result.unity — escena vacía registrada en Build Settings (checkpoint)
</objective>

<execution_context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-PATTERNS.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-RESEARCH.md
</execution_context>

<context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/ROADMAP.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-CONTEXT.md

<interfaces>
<!-- Contratos actuales a extender. Extraídos de los archivos fuente. -->

DeliveredObjectsStore.cs (existente — agregar TakenCount):
  static readonly HashSet<string> takenIds = new HashSet<string>();
  public static void MarkTaken(string id)
  public static bool IsTaken(string id)
  public static void Clear()
  -- FALTA: public static int TakenCount => takenIds.Count;

HUDController.cs (existente — agregar SetVisible):
  private static HUDController instance;
  private void Awake() { ... DontDestroyOnLoad(gameObject); ... }
  -- FALTA: public static void SetVisible(bool visible)

DrunkLevelStore.cs (patrón de referencia para stores nuevos):
  public static int AlcoholLevel { get; private set; } = 0;
  public static void Save(int level) { AlcoholLevel = Mathf.Max(0, level); }
  public static void Clear() { AlcoholLevel = 0; }

CarStateStore.cs (patrón alternativo con guard):
  public static bool HasSavedState { get; private set; }
  public static void Save(Transform car) { ... HasSavedState = true; }
  public static void Clear() { HasSavedState = false; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear stores nuevos (GameResultStore, HomeObjectsTotalStore) y componente LethalObstacle</name>
  <files>
    Assets/_Project/Gameplay/Systems/LethalObstacle.cs
    Assets/_Project/Core/SceneManagement/GameResultStore.cs
    Assets/_Project/Core/SceneManagement/HomeObjectsTotalStore.cs
  </files>
  <read_first>
    - 03-PATTERNS.md §"GameResultStore" y §"HomeObjectsTotalStore" — patrones exactos con DrunkLevelStore y CarStateStore como análogos
    - 03-PATTERNS.md §"LethalObstacle" — patrón análogo SpawnPoint.cs (componente marcador mínimo)
    - 03-RESEARCH.md §"Code Examples" — excerpts de GameResultStore, HomeObjectsTotalStore y LethalObstacle
    - Assets/_Project/Core/SceneManagement/DrunkLevelStore.cs — estructura canónica de store (líneas 1-27)
    - Assets/_Project/Core/SceneManagement/CarStateStore.cs — patrón con guard
    - Assets/_Project/Core/SceneManagement/SpawnPoint.cs — patrón de componente marcador
    - CLAUDE.md — namespace global, [SerializeField] private, comentarios en español, sin namespace
  </read_first>
  <action>
    Crear LethalObstacle.cs en Assets/_Project/Gameplay/Systems/ (la carpeta puede no existir; crearla). Declarar el enum ObstacleCategory { Casa, Arbol, Nino, Mascota } en el mismo archivo antes de la clase. La clase LethalObstacle : MonoBehaviour tiene un solo campo [SerializeField] private ObstacleCategory category = ObstacleCategory.Casa; y una propiedad pública Category => category. Agregar también un método público void SetCategory(ObstacleCategory c) { category = c; } para que CityBuilder pueda asignar la categoría por código (sin reflexión). Sin Awake ni Update. Solo using UnityEngine.

    Crear GameResultStore.cs en Assets/_Project/Core/SceneManagement/. Declarar el enum GameResult { None, Victory, Defeat } en el mismo archivo antes de la clase estática (per D-12). La clase pública estática GameResultStore tiene: propiedad public static GameResult Result { get; private set; } = GameResult.None; método public static void Set(GameResult result) { Result = result; } y método public static void Clear() con comentario "util al empezar una partida nueva" que setea Result = GameResult.None. Sin using UnityEngine (no usa Mathf). Seguir exactamente el estilo de DrunkLevelStore.cs.

    Crear HomeObjectsTotalStore.cs en Assets/_Project/Core/SceneManagement/. Clase estática pública con: propiedad public static int Total { get; private set; } = 0; método public static void Set(int total) con guard if (total >= 0) Total = total; y método public static void Clear() { Total = 0; } con el mismo comentario de "partida nueva". Sin using UnityEngine. Seguir el patrón de CarStateStore.cs.

    Convenciones obligatorias en los tres archivos: sin namespace, comentarios XML en español en todos los métodos públicos, prefijo [NombreClase] en cualquier Debug.Log.
  </action>
  <verify>
    Abrir el Editor Unity 6000.3.11f1 y usar UnityMCP read_console para confirmar que no hay errores de compilación. Los tres archivos deben compilar sin errores ni warnings rojos.
  </verify>
  <acceptance_criteria>
    - LethalObstacle.cs existe en Assets/_Project/Gameplay/Systems/, compila, tiene el enum ObstacleCategory con las 4 categorías y el método SetCategory(ObstacleCategory)
    - GameResultStore.cs existe en Assets/_Project/Core/SceneManagement/, enum GameResult con None/Victory/Defeat, métodos Set(GameResult) y Clear()
    - HomeObjectsTotalStore.cs existe en Assets/_Project/Core/SceneManagement/, propiedad Total (int), métodos Set(int) con guard y Clear()
    - read_console no muestra errores de compilación tras domain reload
  </acceptance_criteria>
  <done>Los tres archivos compilados y disponibles como tipos en el proyecto Unity.</done>
</task>

<task type="auto">
  <name>Task 2: Extender DeliveredObjectsStore (TakenCount) y HUDController (SetVisible)</name>
  <files>
    Assets/_Project/Core/SceneManagement/DeliveredObjectsStore.cs
    Assets/_Project/UI/HUD/HUDController.cs
  </files>
  <read_first>
    - 03-PATTERNS.md §"Modificación: DeliveredObjectsStore" — propiedad TakenCount a agregar (línea exacta después de IsTaken)
    - 03-PATTERNS.md §"Modificación: HUDController" — método SetVisible con patrón instance null-check (líneas 383-403 del PATTERNS)
    - Assets/_Project/Core/SceneManagement/DeliveredObjectsStore.cs — ver HashSet takenIds existente
    - Assets/_Project/UI/HUD/HUDController.cs — ver campo private static HUDController instance y patrón DontDestroyOnLoad
    - 03-RESEARCH.md §"Pitfall 4" — HUD visible encima de Result; la solución es SetVisible
  </read_first>
  <action>
    En DeliveredObjectsStore.cs agregar exactamente una línea después del método IsTaken, antes de Clear(): la propiedad public static int TakenCount => takenIds.Count; con un comentario XML en español "Cantidad de objetos tomados en esta partida." No modificar ninguna otra línea del archivo.

    En HUDController.cs agregar el método público estático SetVisible(bool visible) después del método OnDestroy o al final de la clase, antes del cierre de la llave. El método debe: verificar if (instance != null) y luego llamar instance.gameObject.SetActive(visible). Agregar comentario XML en español: "Muestra u oculta el HUD. Usado por ResultScreenController al entrar a la escena Result." No modificar ningún otro método existente.

    Per D-10: ResultScreenController llamará HUDController.SetVisible(false) al entrar a Result. SetVisible(true) no es necesario llamarlo explícitamente al salir (NewGame carga Home y el HUDController ya existe con DontDestroyOnLoad activo y gameObject activo).
  </action>
  <verify>
    read_console en Editor sin errores de compilación tras domain reload. Verificar en el Inspector que HUDController sigue funcionando en Play mode (la barra de borrachera se llena al tomar).
  </verify>
  <acceptance_criteria>
    - DeliveredObjectsStore.TakenCount compila como propiedad pública estática que retorna takenIds.Count
    - HUDController.SetVisible(bool) compila y está accesible como método estático público
    - El resto de HUDController permanece sin cambios (barra lerp, dinero TMP, DontDestroyOnLoad, OnSceneLoaded)
    - read_console sin errores ni warnings de compilación relacionados con estos dos archivos
  </acceptance_criteria>
  <done>DeliveredObjectsStore.TakenCount y HUDController.SetVisible(bool) disponibles para los planes siguientes.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Crear escena Result.unity y registrarla en Build Settings</name>
  <what-built>
    Los 5 scripts de los tasks anteriores compilan y están listos. La escena Result.unity
    no puede crearse por código — requiere acción en el Editor Unity.
  </what-built>
  <how-to-verify>
    Pasos exactos:
    1. En el Editor Unity, ir a File → New Scene. Seleccionar la plantilla "Basic (Built-in)" o "Empty".
    2. Ir a File → Save As... y guardar como Assets/Scenes/Result.unity (mismo directorio que Home.unity, City.unity, Bar.unity).
    3. Ir a File → Build Settings. Hacer click en "Add Open Scenes" con la escena Result abierta y activa. Verificar que aparece en la lista de escenas (al lado de Home, City, Bar).
    4. Cerrar Build Settings.
    5. Confirmar que Assets/Scenes/Result.unity.meta fue generado por Unity (aparece en el Project panel).
    6. Guardar la escena con Ctrl+S.
    NOTA: No editar EditorBuildSettings.asset a mano — hacerlo desde Build Settings del Editor para evitar conflictos (per RESEARCH.md §Open Items #2).
  </how-to-verify>
  <resume-signal>Escribir "escena creada" cuando Result.unity esté en Build Settings y guardada.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna) | Juego local single-player, sin red ni input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | (n/a) | stores estáticos | accept | Juego offline single-player; los stores viven en memoria del proceso; no hay entrada de red, autenticación ni datos sensibles. Sin vectores de amenaza aplicables. |
| T-03-SC | Tampering | npm/pip installs | accept | Esta fase no instala ningún paquete externo (confirmado en RESEARCH.md §Standard Stack). Sin riesgo de supply chain. |
</threat_model>

<verification>
Al completar los tres tasks:
1. read_console en Editor: cero errores de compilación.
2. Los 5 tipos nuevos/modificados son visibles en el autocompletar de MonoDevelop/Rider: LethalObstacle, GameResultStore, HomeObjectsTotalStore, DeliveredObjectsStore.TakenCount, HUDController.SetVisible.
3. Assets/Scenes/Result.unity existe y aparece en File → Build Settings.
</verification>

<success_criteria>
- 3 scripts nuevos compilan sin errores (LethalObstacle, GameResultStore, HomeObjectsTotalStore)
- 2 scripts existentes extendidos sin romper funcionalidad previa (DeliveredObjectsStore, HUDController)
- Result.unity existe en Assets/Scenes/ y está en Build Settings
- El juego compila y sigue funcionando en Play mode (bar, alcohol, dinero) sin regresiones
</success_criteria>

<output>
Crear .planning/phases/03-loop-de-victoria-y-derrota/03-01-SUMMARY.md cuando completo.
</output>
