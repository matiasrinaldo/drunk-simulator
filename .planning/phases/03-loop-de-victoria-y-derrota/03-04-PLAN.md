---
phase: 03-loop-de-victoria-y-derrota
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - Assets/_Project/UI/Screens/ResultScreenController.cs
autonomous: true
requirements:
  - GAME-03
  - GAME-04

must_haves:
  truths:
    - "Al entrar a la escena Result con GameResultStore.Result == Victory, el panel muestra fondo verde oscuro (#1A2E1A), título 'LLEGASTE A CASA' en verde (#4CAF50), mensaje 'Vendiste todo y llegaste sano. Por ahora.' y botón 'Jugar de nuevo' (D-07)"
    - "Al entrar a la escena Result con GameResultStore.Result == Defeat, el panel muestra fondo rojo oscuro (#2E1A1A), título 'CHOCASTE' en rojo (#E53935), mensaje 'El alcohol ganó esta vez. Siempre hay otra.' y botón 'Reintentar' (D-07)"
    - "El cursor está visible y libre (Cursor.lockState = None) al entrar a Result (D-10)"
    - "Time.timeScale == 1f al entrar a Result (D-10)"
    - "El HUD (HUDController) no es visible durante la pantalla de resultado"
    - "El botón Reintentar/Jugar de nuevo llama NewGame() y limpia todos los stores antes de cargar Home (D-09)"
    - "El botón Salir llama Application.Quit() (y agrega un Debug.Log en el Editor) (D-08)"
    - "Panel centrado (640x360 px), overlay oscuro detrás (#000000 alpha 0.55), CanvasScaler 1920x1080 ScaleWithScreenSize 0.5"
  artifacts:
    - path: "Assets/_Project/UI/Screens/ResultScreenController.cs"
      provides: "MonoBehaviour que construye el Canvas de resultado por código y conecta los botones"
      exports: ["Start", "BuildResultUI"]
  key_links:
    - from: "ResultScreenController.Start()"
      to: "GameResultStore.Result"
      via: "lectura directa del flag estático para determinar qué mostrar"
    - from: "ResultScreenController"
      to: "HUDController.SetVisible(false)"
      via: "llamado en Start() antes de construir la UI"
    - from: "botón Reintentar/Jugar de nuevo"
      to: "GameManager.NewGame()"
      via: "onClick.AddListener por código"
---

<objective>
Construir la pantalla de resultado completa (ResultScreenController) según la
UI-SPEC aprobada: Victory verde / Defeat rojo, textos exactos, dos botones con
acciones reales, cursor libre, HUD oculto.

Purpose: Cierra GAME-03 y GAME-04 completamente y finaliza el loop de gameplay.
Sin este plan la escena Result existe pero está vacía; con este plan el jugador
ve una pantalla distinguible y puede reiniciar o salir.

Output:
- ResultScreenController.cs — MonoBehaviour en Result.unity que construye el Canvas
  por código, lee GameResultStore, aplica colores y copy exactos, conecta botones.
</objective>

<execution_context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-UI-SPEC.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-PATTERNS.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-RESEARCH.md
</execution_context>

<context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/ROADMAP.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-CONTEXT.md

<interfaces>
<!-- Contratos disponibles tras 03-01..03-03. -->

GameResultStore.cs (creado en 03-01):
  public enum GameResult { None, Victory, Defeat }
  public static GameResult Result { get; private set; }
  public static void Clear()

HUDController.cs (modificado en 03-01):
  public static void SetVisible(bool visible)   // oculta/muestra el Canvas del HUD

GameManager.cs (creado en 03-02):
  public static void NewGame()   // limpia todos los stores y carga Home

HUDController.cs (referencia de construcción UI por código — mismo patrón a replicar):
  // Canvas, CanvasScaler, GraphicRaycaster por AddComponent
  // RectTransform con anchorMin/Max, pivot, anchoredPosition, sizeDelta
  // TextMeshProUGUI con font cargada desde Resources ("Fonts & Materials/LiberationSans SDF")
  // Shadow component con effectDistance = (1, -1)
  // Button con ColorBlock configurado por código

Paleta de la UI-SPEC (valores RGBA Unity exactos):
  Victory panel:      new Color(0.102f, 0.180f, 0.102f, 0.92f)  // #1A2E1A
  Defeat panel:       new Color(0.180f, 0.102f, 0.102f, 0.92f)  // #2E1A1A
  Victory title:      new Color(0.298f, 0.686f, 0.314f, 1.0f)   // #4CAF50
  Defeat title:       new Color(0.898f, 0.224f, 0.208f, 1.0f)   // #E53935
  Victory retry btn:  new Color(0.220f, 0.557f, 0.235f, 1.0f)   // #388E3C
  Defeat retry btn:   new Color(0.773f, 0.157f, 0.157f, 1.0f)   // #C62828
  Quit button:        new Color(0.380f, 0.380f, 0.380f, 1.0f)   // #616161
  Overlay:            new Color(0.0f, 0.0f, 0.0f, 0.55f)        // #000000 alpha 0.55
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear ResultScreenController con UI por código según UI-SPEC</name>
  <files>
    Assets/_Project/UI/Screens/ResultScreenController.cs
  </files>
  <read_first>
    - 03-UI-SPEC.md — COMPLETO: jerarquía Canvas → Panel → TitleText / MessageText / ButtonGroup, dimensiones exactas (Panel 640x360, TitleText 576x80, MessageText 576x60, ButtonGroup 576x112), colores por estado, copy exacto (Victory/Defeat), ColorBlock de botones, spacing tokens (md=16px, xl=32px, 2xl=48px), CanvasScaler 1920x1080 0.5
    - 03-PATTERNS.md §"ResultScreenController" — análogo HUDController.cs: patrón de imports, Canvas por código, font con fallback Resources.Load, Shadow, AddListener por código, NO DontDestroyOnLoad
    - 03-RESEARCH.md §"Code Examples — ResultScreenController" — estructura de Start() con Cursor, HUD, GameResultStore, botones
    - 03-RESEARCH.md §"Pitfall 4" — HUD visible sobre Result: llamar HUDController.SetVisible(false) en Start()
    - Assets/_Project/UI/HUD/HUDController.cs líneas 50-138 — patrón exacto de construcción Canvas por código (Canvas, CanvasScaler, GraphicRaycaster, RectTransform anclajes, TextMeshProUGUI, Shadow, Sprite sólido)
    - 03-UI-SPEC.md §"Copywriting Contract" — copy exacto de los cuatro campos por estado (Victoria / Derrota)
    - CLAUDE.md — namespace global, TMP, comentarios en español
  </read_first>
  <action>
    Crear ResultScreenController.cs en Assets/_Project/UI/Screens/ (la carpeta puede no existir; crearla). MonoBehaviour sin DontDestroyOnLoad. Imports: using TMPro, using UnityEngine, using UnityEngine.SceneManagement, using UnityEngine.UI.

    En Start() hacer en este orden: (1) Cursor.visible = true; Cursor.lockState = CursorLockMode.None; Time.timeScale = 1f; (per D-10). (2) HUDController.SetVisible(false); (per Pitfall 4). (3) Llamar BuildResultUI(); (4) Conectar botones con AddListener.

    En BuildResultUI(): construir por código la jerarquía descrita en la UI-SPEC. La carpeta de objetos:

    Canvas "ResultCanvas" con renderMode ScreenSpaceOverlay, sortingOrder 10, CanvasScaler ScaleWithScreenSize 1920x1080 matchWidthOrHeight 0.5, GraphicRaycaster.

    Overlay: Image hijo del Canvas que ocupa toda la pantalla (anchorMin 0,0 / anchorMax 1,1 / offsetMin y offsetMax a 0) con color new Color(0f, 0f, 0f, 0.55f).

    ResultPanel: RectTransform hijo del Canvas con anchorMin (0.5,0.5), anchorMax (0.5,0.5), pivot (0.5,0.5), anchoredPosition (0,0), sizeDelta (640,360). Image con color según estado: Victory → new Color(0.102f, 0.180f, 0.102f, 0.92f); Defeat → new Color(0.180f, 0.102f, 0.102f, 0.92f).

    TitleText (TextMeshProUGUI) hijo del panel: anchoredPosition apuntando a parte superior (ejemplo: (0, 100) sobre el pivote central), sizeDelta (576, 80). Font: Resources.Load("Fonts & Materials/LiberationSans SDF") con fallback "LiberationSans SDF" — exactamente igual que HUDController. fontSize 52, fontStyle FontStyles.Bold, alignment TextAlignmentOptions.Center. Color según estado. Texto: Victory → "LLEGASTE A CASA"; Defeat → "CHOCASTE" — asignar con .ToUpper() o directamente en mayúsculas. Agregar Shadow con effectDistance (1,-1) y effectColor Color.black — solo al TitleText.

    MessageText (TextMeshProUGUI) hijo del panel: sizeDelta (576, 60), 16px debajo del TitleText (usar anchoredPosition calculado). fontSize 24, fontStyle FontStyles.Normal, alignment TextAlignmentOptions.Center, color Color.white. Texto: Victory → "Vendiste todo y llegaste sano. Por ahora."; Defeat → "El alcohol ganó esta vez. Siempre hay otra."

    ButtonGroup: RectTransform hijo del panel, sizeDelta (576, 112), 32px debajo de MessageText. Agregar VerticalLayoutGroup con spacing 8, childForceExpandWidth true, childControlWidth true.

    RetryButton: Button hijo del ButtonGroup con RectTransform sizeDelta (576, 48). Image color según estado: Victory → new Color(0.220f, 0.557f, 0.235f, 1f); Defeat → new Color(0.773f, 0.157f, 0.157f, 1f). Configurar ColorBlock vía Button.colors: cb.highlightedColor = Color.Lerp(colorBase, Color.white, 0.15f); cb.pressedColor = Color.Lerp(colorBase, Color.black, 0.15f); cb.fadeDuration = 0.1f. Texto hijo TextMeshProUGUI: fontSize 20, fontStyle Bold, color blanco. Texto: Victory → "Jugar de nuevo"; Defeat → "Reintentar".

    QuitButton: Button hijo del ButtonGroup, igual estructura, color new Color(0.380f, 0.380f, 0.380f, 1f) en ambos estados. Texto "Salir".

    Listeners: retryButton.onClick.AddListener(() => GameManager.NewGame()); quitButton.onClick.AddListener(() => { Debug.Log("[ResultScreen] Salir presionado."); Application.Quit(); });

    Usar la misma helper CreateSolidSprite() de HUDController (o duplicar el método privado) para garantizar que los Image de fondo se rendericen correctamente. No importar HUDController directamente — duplicar el método privado estático en ResultScreenController.
  </action>
  <verify>
    Play en City.unity: conducir hacia un edificio → chocar → escena Result carga. Verificar:
    1. Pantalla muestra fondo rojo oscuro, título "CHOCASTE", mensaje de derrota, dos botones.
    2. El cursor está visible.
    3. El HUD (barra de borrachera + dinero) NO es visible.
    4. Click en "Reintentar" → escena Home carga, objetos presentes, HUD visible nuevamente.
    5. Click en "Salir" → Console muestra "[ResultScreen] Salir presionado."
    Para Victory: en debug setear los stores y llegar a Home con condición cumplida → pantalla verde con "LLEGASTE A CASA".
  </verify>
  <acceptance_criteria>
    - ResultScreenController.cs compila sin errores
    - En modo Defeat: panel color #2E1A1A (rojo oscuro), TitleText "CHOCASTE" en #E53935, MessageText correcto en blanco, botón "Reintentar" en #C62828
    - En modo Victory: panel color #1A2E1A (verde oscuro), TitleText "LLEGASTE A CASA" en #4CAF50, MessageText correcto en blanco, botón "Jugar de nuevo" en #388E3C
    - Cursor visible y libre (CursorLockMode.None) en Result
    - HUD no visible durante la pantalla de resultado
    - Botón Reintentar/Jugar de nuevo: llamar GameManager.NewGame() → stores limpios → Home cargado con todos los objetos y alcohol=0
    - Botón Salir: Debug.Log en Console + Application.Quit()
    - CanvasScaler 1920x1080 con ScaleWithScreenSize 0.5
    - TitleText tiene Shadow; MessageText NO tiene Shadow
    - Panel centrado en pantalla con overlay oscuro detrás
  </acceptance_criteria>
  <done>ResultScreenController completo: Victory y Defeat distinguibles visualmente, botones funcionales, loop cerrado.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar ResultScreenController a la escena Result.unity</name>
  <files>
    Assets/Scenes/Result.unity
  </files>
  <read_first>
    - 03-VALIDATION.md §"Wave 0 Requirements" — la escena Result.unity debe existir antes de referenciarla (creada en 03-01 checkpoint)
    - 03-RESEARCH.md §"Open Items #2" — Result.unity ya fue creada en 03-01; solo agregar el componente
    - 03-PATTERNS.md §"ResultScreenController" — NO usa DontDestroyOnLoad; vive en la escena Result
  </read_first>
  <action>
    Usar UnityMCP para abrir la escena Result.unity (Assets/Scenes/Result.unity). Crear un GameObject vacío "ResultController" en la raíz de la escena. Agregar el componente ResultScreenController al GameObject "ResultController". Guardar la escena con Ctrl+S.

    Si la escena no tiene cámara (fue creada como Empty), agregar una Camera básica con Main Camera tag y un Directional Light (para que Unity no muestre advertencias de cámara faltante).

    Verificar con UnityMCP o manualmente que el componente ResultScreenController aparece en el Inspector del GameObject "ResultController" en la escena Result.

    Nota: ResultScreenController construye su propio Canvas en Start() — no hay que agregar Canvas, Image ni TextMeshProUGUI al Scene manualmente.
  </action>
  <verify>
    Play mode end-to-end (flujo completo per 03-VALIDATION.md):
    1. Abrir City.unity en Play. Entrar al auto. Conducir hacia un edificio y chocar.
    2. Escena Result carga. Aparece la pantalla de Derrota con panel rojo, título "CHOCASTE", mensaje, dos botones. Cursor visible. HUD oculto.
    3. Click en "Reintentar" → Home carga. Los 6 objetos están presentes. DrunkLevelStore.AlcoholLevel == 0. PlayerMoneyStore.Money == 0. HUD visible.
    4. (Opcional) Setear stores de victoria via debug y conducir a Home → pantalla Victory verde.

    Criterio 5 del Roadmap: play sin chocar ni vender todo → llegar a Home → Home carga normalmente, sin pantalla de resultado.
  </verify>
  <acceptance_criteria>
    - Result.unity tiene un GameObject "ResultController" con componente ResultScreenController
    - Play completo en City → choque → Result: pantalla de Derrota visible, distinguible, con botones funcionales
    - Reintentar desde Result: Home carga con todos los objetos, alcohol=0, dinero=0 (todos los stores limpios)
    - Play normal sin condición de victoria/derrota: ninguna pantalla de resultado aparece
    - GameResultStore.Result == None en una partida normal sin fin de juego
    - read_console sin errores de compilación ni NullReferenceException durante el flujo completo
  </acceptance_criteria>
  <done>Loop de gameplay completo: victoria y derrota con pantallas distinguibles, Reintentar funcional, criterio 5 verificado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna) | Juego local single-player; la UI de resultado no expone datos de usuario ni red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | (n/a) | ResultScreenController | accept | Juego offline; la pantalla de resultado es UI local pura. Sin entrada de red, credenciales ni datos sensibles. Sin vectores de amenaza aplicables. |
| T-03-SC | Tampering | npm/pip installs | accept | No se instalan paquetes externos en este plan. |
</threat_model>

<verification>
Al completar los dos tasks (validación completa per 03-VALIDATION.md):

GAME-01: Chocar con edificio/árbol/niño/mascota mientras IsControlled=true → pantalla Defeat visible en Result. [Manual Play]
GAME-01 negativo: Caminar contra edificio (IsControlled=false) → sin pantalla. [Manual Play]
GAME-02: Vender todo + alcohol>=6 + llegar a Home manejando → pantalla Victory. [Manual Play]
GAME-02 negativo: Sin vender todo → llegar a Home → Home normal. [Manual Play]
GAME-03: Pantalla Victory verde, "LLEGASTE A CASA", dos botones, cursor visible. [Manual Play]
GAME-04: Pantalla Defeat roja, "CHOCASTE", dos botones, cursor visible. [Manual Play]
D-09/D-13: Reintentar → Home con objetos presentes, alcohol=0, dinero=0. [Manual Play]
D-10: Cursor visible + timeScale=1 en Result. [Manual Play]
Criterio 5: Partida normal sin chocar ni vender todo → sin pantalla de resultado. [Manual Play]
</verification>

<success_criteria>
Los 5 success criteria del Roadmap Phase 3 verificados en Play mode:
1. Chocar mientras se maneja → pantalla de derrota distinguible
2. Pantalla de derrota con texto/color distinto y al menos un botón de acción (Reintentar/Salir)
3. Vender todo + alcohol mínimo + llegar a Home → pantalla de victoria
4. Pantalla de victoria distinguible con mensaje de éxito y al menos un botón de acción
5. Partida normal sin chocar ni completar condición → ninguna pantalla de resultado aparece
</success_criteria>

<output>
Crear .planning/phases/03-loop-de-victoria-y-derrota/03-04-SUMMARY.md cuando completo.
</output>
