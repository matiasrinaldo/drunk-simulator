---
phase: 03-loop-de-victoria-y-derrota
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Assets/_Project/Core/Managers/GameManager.cs
  - Assets/_Project/Gameplay/Vehicles/CarController.cs
  - Assets/_Project/Editor/CityBuilder.cs
autonomous: true
requirements:
  - GAME-01
  - GAME-04

must_haves:
  truths:
    - "Al chocar con un edificio mientras el auto está controlado (IsControlled=true), el auto se frena, SetControlled(false) se llama, y SceneManager carga 'Result' (D-02, D-03)"
    - "Al chocar a pie (IsControlled=false), no ocurre ninguna transición de escena (D-02)"
    - "GameManager recibe la señal de choque vía OnCarCrash() y setea GameResultStore.Result = Defeat antes de cargar Result (D-11)"
    - "CityBuilder agrega LethalObstacle a cada edificio y árbol generado al correr Build City Layout (D-01)"
    - "Niño y mascota existen en City.unity como GameObjects con BoxCollider no-trigger y componente LethalObstacle (D-01)"
    - "minAlcoholRequired = 6 es un campo [SerializeField] en GameManager (D-05)"
    - "NewGame() llama Clear() en los 7 stores y setea PlayerSpawner.NextSpawnId = null antes de cargar Home (D-09, D-13)"
  artifacts:
    - path: "Assets/_Project/Core/Managers/GameManager.cs"
      provides: "MonoBehaviour manager con OnCarCrash() y OnPlayerArrivedHome() (stub) y NewGame() estático"
      exports: ["OnCarCrash", "OnPlayerArrivedHome", "NewGame"]
    - path: "Assets/_Project/Gameplay/Vehicles/CarController.cs"
      provides: "OnCollisionEnter agregado — frena + llama GameManager.OnCarCrash()"
    - path: "Assets/_Project/Editor/CityBuilder.cs"
      provides: "PlaceBuilding y PlaceTree agregan LethalObstacle al GameObject generado"
  key_links:
    - from: "CarController.OnCollisionEnter"
      to: "GameManager.OnCarCrash()"
      via: "FindFirstObjectByType<GameManager>() con null-check"
    - from: "GameManager.OnCarCrash()"
      to: "SceneManager.LoadSceneAsync('Result')"
      via: "GameResultStore.Set(GameResult.Defeat)"
---

<objective>
Implementar el slice de derrota completo end-to-end: chocar con un obstáculo letal
mientras se maneja → escena Result con el flag Defeat seteado.

Purpose: Cierra GAME-01 y GAME-04. Crea GameManager que el plan 03-03 extiende para
la victoria. Marca los obstáculos letales en CityBuilder y agrega los peatones
niño/mascota en City.

Output:
- GameManager.cs nuevo — recibe señal de choque, setea Defeat, carga Result
- CarController.cs modificado — OnCollisionEnter con guard IsControlled
- CityBuilder.cs modificado — PlaceBuilding y PlaceTree agregan LethalObstacle
- Niño y mascota colocados manualmente en City.unity (via UnityMCP o Editor)
</objective>

<execution_context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-PATTERNS.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-RESEARCH.md
</execution_context>

<context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/ROADMAP.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-CONTEXT.md

<interfaces>
<!-- Contratos disponibles tras 03-01. Extraídos del codebase. -->

LethalObstacle.cs (creado en 03-01):
  public enum ObstacleCategory { Casa, Arbol, Nino, Mascota }
  public class LethalObstacle : MonoBehaviour
  {
      [SerializeField] private ObstacleCategory category = ObstacleCategory.Casa;
      public ObstacleCategory Category => category;
      public void SetCategory(ObstacleCategory c) { category = c; }
  }

GameResultStore.cs (creado en 03-01):
  public enum GameResult { None, Victory, Defeat }
  public static class GameResultStore
  {
      public static GameResult Result { get; private set; } = GameResult.None;
      public static void Set(GameResult result) { Result = result; }
      public static void Clear() { Result = GameResult.None; }
  }

CarController.cs (existente — agregar OnCollisionEnter):
  [RequireComponent(typeof(Rigidbody))]
  public class CarController : MonoBehaviour
  {
      private Rigidbody rb;
      private bool isControlled;
      public bool IsControlled => isControlled;
      public void SetControlled(bool value) { isControlled = value; ... }
      // rb.linearVelocity = Vector3.zero; rb.angularVelocity = Vector3.zero; — usado en Start()
      // FindFirstObjectByType<DrunkManager>(); — patrón ya usado en Awake
  }

CityBuilder.cs (existente — modificar PlaceBuilding y PlaceTree):
  static void PlaceBuilding(Transform parent, string goName, string fbxName, Vector3 pos, float yRot)
  // Al final del método, luego de asignar go.transform.localScale: agregar LethalObstacle

  static void PlaceTree(Transform parent, string fbxName, int side, float z)
  // Al final del método, luego de asignar go.transform.position: agregar LethalObstacle

DrunkManager.cs (referencia de patrón para GameManager):
  using System; using UnityEngine;
  public class DrunkManager : MonoBehaviour { [Header("Config")] [SerializeField] ... }
  // Patrón: FindFirstObjectByType con fallback null-check en métodos públicos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear GameManager con OnCarCrash() y stub OnPlayerArrivedHome() + NewGame() estático</name>
  <files>
    Assets/_Project/Core/Managers/GameManager.cs
  </files>
  <read_first>
    - 03-PATTERNS.md §"GameManager" — análogo DrunkManager.cs: patrón de imports, [Header], [SerializeField], guard clauses, FindFirstObjectByType
    - 03-RESEARCH.md §"Code Examples — GameManager" — método OnPlayerArrivedHome y OnCarCrash completos
    - 03-RESEARCH.md §"Code Examples — NewGame()" — lista completa de stores a limpiar (D-13)
    - Assets/_Project/Core/Managers/DrunkManager.cs — estructura completa para replicar estilo
    - 03-CONTEXT.md D-11 — GameManager per-escena en City (no DontDestroyOnLoad); D-13 — NewGame() llama Clear() en todos los stores
    - CLAUDE.md — namespace global, [Header] en español, comentarios en español
  </read_first>
  <action>
    Crear GameManager.cs en Assets/_Project/Core/Managers/ como MonoBehaviour (no singleton, no DontDestroyOnLoad — vive per-escena en City per D-11). Agregar using UnityEngine y using UnityEngine.SceneManagement.

    Campos: [Header("Condicion de victoria")] [SerializeField] private int minAlcoholRequired = 6; con [Tooltip("Nivel minimo de alcohol requerido para ganar (D-05).")].

    Método público OnCarCrash(): setear GameResultStore.Set(GameResult.Defeat) y llamar SceneManager.LoadSceneAsync("Result"). Sin guard de IsControlled — esa comprobación ya está en CarController.OnCollisionEnter (D-02).

    Método público OnPlayerArrivedHome(): en este plan, implementar la lógica completa de evaluación de victoria (no stub): leer HomeObjectsTotalStore.Total, DeliveredObjectsStore.TakenCount y DrunkLevelStore.AlcoholLevel, evaluar las dos condiciones (allDelivered y drunkEnough), si ambas se cumplen llamar GameResultStore.Set(GameResult.Victory) + SceneManager.LoadSceneAsync("Result"), de lo contrario SceneManager.LoadSceneAsync("Home"). Guard: si HomeObjectsTotalStore.Total == 0, cargar Home directamente (partida no inicializada, per Pitfall 5).

    Método public static void NewGame(): llamar Clear() de CarStateStore, DeliveredObjectsStore, DrunkLevelStore, PlayerMoneyStore, HeldObjectStore, HomeObjectsTotalStore, GameResultStore; setear PlayerSpawner.NextSpawnId = null; llamar SceneManager.LoadSceneAsync("Home"). Agregar comentario XML: "Reset completo de partida (D-13). Llama Clear() de todos los stores y carga Home desde cero."

    El GameManager no tiene Awake ni Update — solo los tres métodos. Se agrega al GameObject de City.unity manualmente (no requiere auto-arranque; el CityHomeDoorTrigger y el CarController lo buscan por FindFirstObjectByType en el mismo frame).

    Referenciar en el código: HeldObjectStore (ya existe en el proyecto), PlayerMoneyStore (ya existe), agregar los using necesarios. Dejar un comentario en NewGame(): "// TODO: agregar HUDController.SetVisible(true) si fuera necesario al reiniciar."
  </action>
  <verify>
    read_console en Editor: sin errores de compilación. GameManager aparece en el menú "Add Component" del Inspector de Unity.
  </verify>
  <acceptance_criteria>
    - GameManager.cs compila sin errores en el Editor
    - El tipo GameManager es reconocido (aparece en autocompletar y en Add Component)
    - OnCarCrash() existe como método público que setea GameResult.Defeat
    - OnPlayerArrivedHome() existe como método público con lógica de evaluación completa
    - NewGame() existe como método estático público que llama Clear() en los 7 stores y carga Home
    - minAlcoholRequired es [SerializeField] con valor default 6
    - No usa DontDestroyOnLoad ni [RuntimeInitializeOnLoadMethod]
  </acceptance_criteria>
  <done>GameManager.cs compilado con la lógica de derrota, victoria (completa) y reset.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar OnCollisionEnter a CarController + LethalObstacle en CityBuilder + peatones en City</name>
  <files>
    Assets/_Project/Gameplay/Vehicles/CarController.cs
    Assets/_Project/Editor/CityBuilder.cs
  </files>
  <read_first>
    - 03-PATTERNS.md §"Modificación: CarController" — patrón OnCollisionEnter con guard isControlled, freno de emergencia (rb.linearVelocity/angularVelocity = Vector3.zero), FindFirstObjectByType<GameManager>
    - 03-PATTERNS.md §"Modificación: CityBuilder" — patrón PlaceBuilding al final (go.AddComponent<LethalObstacle>()) y PlaceTree con SetCategory(ObstacleCategory.Arbol)
    - 03-RESEARCH.md §"Pitfall 1" — MeshColliders no-convex: si aparece error, los MeshColliders de ruedas deben marcarse isTrigger=true
    - 03-RESEARCH.md §"Pitfall 2" — los FBX de CharacterKit no tienen Collider: agregar BoxCollider/CapsuleCollider manualmente
    - 03-RESEARCH.md §"Open Items #1" — niño: usar character-X.fbx del CharacterKit; mascota: primitiva geométrica (cubo a escala pequeña)
    - Assets/_Project/Gameplay/Vehicles/CarController.cs — estructura actual: rb campo privado, isControlled campo privado, Start() ya usa rb.linearVelocity = Vector3.zero
    - Assets/_Project/Editor/CityBuilder.cs — PlaceBuilding líneas 176-199, PlaceTree líneas 220-240
    - CLAUDE.md — namespace global, comentarios en español
  </read_first>
  <action>
    En CarController.cs, agregar el método private void OnCollisionEnter(Collision collision) al final de la clase (antes del cierre de la llave). El método debe: (1) guard: if (!isControlled) return; (2) obtener LethalObstacle obstacle = collision.gameObject.GetComponent<LethalObstacle>(); (3) guard: if (obstacle == null) return; (4) frenar: rb.linearVelocity = Vector3.zero; rb.angularVelocity = Vector3.zero; (5) bloquear control: SetControlled(false); (6) notificar: var gm = FindFirstObjectByType<GameManager>(); if (gm != null) gm.OnCarCrash(); Agregar comentario en español sobre D-03 y D-02. No modificar ningún otro método.

    En CityBuilder.cs, al final de PlaceBuilding() (después de la línea go.transform.localScale = Vector3.one * BuildingScale;) agregar: go.AddComponent<LethalObstacle>(); — el default del enum (Casa) es correcto para edificios. Al final de PlaceTree() (después de go.transform.position = new Vector3(xPos, 0f, z);) agregar: var lo = go.AddComponent<LethalObstacle>(); lo.SetCategory(ObstacleCategory.Arbol); (requiere que LethalObstacle.SetCategory esté disponible — creado en 03-01). Agregar comentario en español: "Marca como obstáculo letal para la detección de derrota (D-01)."

    Para el peatón "niño" y la mascota en City.unity: usar UnityMCP para abrir City.unity, crear 1-2 GameObjects con un FBX character-X del CharacterKit como mesh (cualquier variante, p. ej. "character-b") con un CapsuleCollider (height 1.8, radius 0.3, isTrigger=false) y el componente LethalObstacle con categoría Nino. Para la mascota, crear un cubo primitivo a escala (0.4, 0.4, 0.8) con BoxCollider no-trigger y LethalObstacle con categoría Mascota. Colocar los GameObjects en el borde de la acera (cerca de Z=-35 para el niño, Z=+15 para la mascota, X=-6 aproximadamente). Guardar la escena. Si el UnityMCP no puede acceder a City.unity mientras está cerrada, abrir la escena desde el Editor y agregar los GameObjects manualmente antes de cerrar.

    IMPORTANTE: Al agregar LethalObstacle en CityBuilder, verificar que la clase CityBuilder tenga acceso al tipo LethalObstacle (está en el namespace global — no necesita using adicional). Verificar en read_console que no haya errores CS0246 tras guardar.
  </action>
  <verify>
    Pasos de Play mode:
    1. Abrir City.unity, presionar Play.
    2. Entrar al auto (tecla E), conducir hacia un edificio. Verificar en Console que no hay "Non-convex MeshCollider" error bloqueante.
    3. Chocar el auto contra el edificio → el auto se detiene, Control bloqueado → escena "Result" se carga (puede mostrar pantalla vacía porque ResultScreenController aún no existe — esto es esperado).
    4. Caminar a pie hacia un edificio sin estar en el auto → NO se carga ninguna escena.
    Verificar también: CityBuilder al correr "Drunk Simulator → Build City Layout" genera edificios y árboles sin errores de consola.
  </verify>
  <acceptance_criteria>
    - CarController.cs compila sin errores; OnCollisionEnter está presente
    - CityBuilder.cs compila sin errores; PlaceBuilding y PlaceTree llaman AddComponent<LethalObstacle>()
    - En Play mode en City: chocar el auto contra un edificio mientras IsControlled=true → rb.linearVelocity se vuelve cero y SceneManager.LoadSceneAsync("Result") se invoca (confirmado en Console con [GameManager] log o carga de escena Result)
    - En Play mode en City: caminar contra un edificio sin estar en el auto → ninguna transición de escena
    - City.unity tiene al menos 1 GameObject "Nino" con CapsuleCollider no-trigger y LethalObstacle.Category=Nino, y 1 "Mascota" con BoxCollider no-trigger y LethalObstacle.Category=Mascota
    - read_console sin errores de compilación
  </acceptance_criteria>
  <done>Slice derrota end-to-end funcional: chocar auto → GameResultStore.Defeat → LoadSceneAsync("Result").</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna) | Juego local single-player; OnCollisionEnter es callback interno de Unity Physics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | (n/a) | GameManager, CarController | accept | Juego offline; OnCollisionEnter es invocado por el motor, no por input externo. Sin superficie de amenaza real. |
| T-03-SC | Tampering | npm/pip installs | accept | No se instalan paquetes externos en este plan. |
</threat_model>

<verification>
Al completar los dos tasks:
1. read_console: cero errores de compilación.
2. Play en City con auto: chocar contra edificio → Console muestra carga de "Result" o mensaje de GameManager.
3. Play en City sin auto: caminar contra edificio → sin transición.
4. City.unity inspeccionada: 1 Nino + 1 Mascota con Collider y LethalObstacle.
5. Correr CityBuilder → edificios y árboles nuevos tienen LethalObstacle en Inspector.
</verification>

<success_criteria>
- GAME-01: chocar contra casa/árbol/niño/mascota mientras IsControlled=true → transición a Result
- GAME-04 (parcial): escena Result se carga con GameResultStore.Result == Defeat (pantalla aún vacía — se completa en 03-04)
- Sin regresiones en el loop económico (vender, comprar bebidas) ni en el HUD
</success_criteria>

<output>
Crear .planning/phases/03-loop-de-victoria-y-derrota/03-02-SUMMARY.md cuando completo.
</output>
