---
phase: 03-loop-de-victoria-y-derrota
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - Assets/_Project/Core/SceneManagement/HomeInitializer.cs
  - Assets/_Project/Core/SceneManagement/CityHomeDoorTrigger.cs
autonomous: true
requirements:
  - GAME-02
  - GAME-03

must_haves:
  truths:
    - "Al cargar Home por primera vez, HomeObjectsTotalStore.Total se establece en 6 (la cantidad real de CarryableObject en la escena) (D-06)"
    - "Al llegar a Home manejando con todos los objetos vendidos y alcohol >= 6, se carga la escena Result con GameResultStore.Result == Victory (D-04)"
    - "Al llegar a Home sin vender todo o sin alcohol suficiente, se carga Home normalmente (sin pantalla de resultado)"
    - "CityHomeDoorTrigger setea triggered=true solo cuando se confirma una transición de escena (previene el bloqueo del trigger)"
  artifacts:
    - path: "Assets/_Project/Core/SceneManagement/HomeInitializer.cs"
      provides: "MonoBehaviour en Home scene que captura el total de CarryableObject y lo setea en HomeObjectsTotalStore"
    - path: "Assets/_Project/Core/SceneManagement/CityHomeDoorTrigger.cs"
      provides: "Trigger modificado que delega a GameManager.OnPlayerArrivedHome() antes de cargar escena"
  key_links:
    - from: "HomeInitializer.Awake()"
      to: "HomeObjectsTotalStore.Total"
      via: "FindObjectsByType<CarryableObject>().Length cuando Total == 0"
    - from: "CityHomeDoorTrigger.OnTriggerEnter"
      to: "GameManager.OnPlayerArrivedHome()"
      via: "FindFirstObjectByType<GameManager>() con fallback al comportamiento anterior"
    - from: "GameManager.OnPlayerArrivedHome()"
      to: "SceneManager.LoadSceneAsync('Result' o 'Home')"
      via: "evaluación allDelivered && drunkEnough"
---

<objective>
Implementar el slice de victoria end-to-end: HomeInitializer captura el total de
CarryableObject en Home, y CityHomeDoorTrigger delega al GameManager la decisión de
cargar Home o Result/Victory.

Purpose: Cierra GAME-02 y GAME-03. GameManager.OnPlayerArrivedHome() ya está
implementado en 03-02 — este plan solo conecta los dos puntos de entrada que lo activan.

Output:
- HomeInitializer.cs nuevo — captura total de CarryableObject en Home.Awake()
- CityHomeDoorTrigger.cs modificado — delega a GameManager en lugar de cargar Home directamente
</objective>

<execution_context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-PATTERNS.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-RESEARCH.md
</execution_context>

<context>
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/ROADMAP.md
@/Users/msancheznovelli/itba/electivas/intro_juegos/drunk-simulator/.planning/phases/03-loop-de-victoria-y-derrota/03-CONTEXT.md

<interfaces>
<!-- Contratos disponibles tras 03-01 y 03-02. -->

GameManager.cs (creado en 03-02):
  public class GameManager : MonoBehaviour
  {
      [SerializeField] private int minAlcoholRequired = 6;
      public void OnPlayerArrivedHome()   // evalúa victoria y carga Result o Home
      public void OnCarCrash()            // setea Defeat y carga Result
      public static void NewGame()        // limpia stores y carga Home
  }

HomeObjectsTotalStore.cs (creado en 03-01):
  public static class HomeObjectsTotalStore
  {
      public static int Total { get; private set; } = 0;
      public static void Set(int total)   // guard: if (total >= 0)
      public static void Clear()
  }

DeliveredObjectsStore.cs (modificado en 03-01):
  public static int TakenCount => takenIds.Count;
  public static void MarkTaken(string id)
  public static bool IsTaken(string id)
  public static void Clear()

CarryableObject.cs (existente — para FindObjectsByType):
  public string StableId  // ID estable del objeto
  // 6 instancias en Home.unity con objectId home-obj-1..home-obj-6

CityHomeDoorTrigger.cs (existente — a modificar):
  [RequireComponent(typeof(Collider))]
  public class CityHomeDoorTrigger : MonoBehaviour
  {
      [SerializeField] private string sceneToLoad = "Home";
      [SerializeField] private string playerTag = "player";
      private bool triggered;
      void OnTriggerEnter(Collider other)
      {
          if (triggered) return;
          if (!other.CompareTag(playerTag)) return;
          triggered = true;
          var car = FindFirstObjectByType<CarController>();
          if (car != null) CarStateStore.Save(car.transform);
          SceneManager.LoadSceneAsync(sceneToLoad);  // ← reemplazar esto
      }
  }

PlayerSpawner.cs (referencia de patrón para HomeInitializer):
  public class PlayerSpawner : MonoBehaviour
  {
      public static string NextSpawnId;
      void Start() { ... FindObjectsByType<SpawnPoint>(FindObjectsSortMode.None) ... }
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear HomeInitializer y agregar a la escena Home</name>
  <files>
    Assets/_Project/Core/SceneManagement/HomeInitializer.cs
  </files>
  <read_first>
    - 03-PATTERNS.md §"HomeInitializer" — análogo PlayerSpawner.cs: FindObjectsByType, guard, store estático
    - 03-RESEARCH.md §"Open Items #3 — DeliveredObjectsStore count vs total" — punto de captura del total: HomeInitializer en Awake, guard Total > 0
    - 03-RESEARCH.md §"Pitfall 5" — si Total == 0 el jugador nunca puede ganar; el guard en HomeInitializer lo previene
    - Assets/_Project/Core/SceneManagement/PlayerSpawner.cs — patrón exacto con FindObjectsByType<SpawnPoint> en Start()
    - Assets/Scenes/Home.unity — 6 CarryableObject con objectId home-obj-1..home-obj-6 (confirmado en RESEARCH)
    - CLAUDE.md — namespace global, comentarios en español, Debug.Log con prefijo [NombreClase]
  </read_first>
  <action>
    Crear HomeInitializer.cs en Assets/_Project/Core/SceneManagement/. MonoBehaviour simple sin namespace. Solo using UnityEngine.

    En Awake() (no Start() — debe ejecutarse antes de que cualquier CarryableObject.Awake() pueda ocultarse): guard if (HomeObjectsTotalStore.Total > 0) return; para capturar solo la primera vez. Luego: var objetos = FindObjectsByType<CarryableObject>(FindObjectsSortMode.None); int total = objetos.Length; HomeObjectsTotalStore.Set(total); Debug.Log($"[HomeInitializer] Total de objetos en Home: {total}."); Si total == 0 agregar Debug.LogWarning con mensaje "No se encontraron CarryableObject en Home — la condicion de victoria es imposible."

    Agregar el componente HomeInitializer a la escena Home.unity: usar UnityMCP para abrir Home.unity, agregar el componente HomeInitializer a algún GameObject raíz de la escena (por ejemplo el mismo GameObject que tiene PlayerSpawner, o crear un vacío "GameInitializer"). Guardar la escena. Si UnityMCP no puede acceder, dejar una instrucción clara en el SUMMARY de que el executor manual debe agregar HomeInitializer a Home.unity.

    No agregar DontDestroyOnLoad — este MonoBehaviour muere con la escena Home y eso es correcto: el total queda en el store estático.
  </action>
  <verify>
    Play en Home.unity → Console muestra "[HomeInitializer] Total de objetos en Home: 6." en el primer frame. Recargar Home → el log NO aparece de nuevo (guard Total > 0 funciona).
  </verify>
  <acceptance_criteria>
    - HomeInitializer.cs compila sin errores
    - Al correr Play en Home.unity, Console muestra el total de CarryableObject (esperado: 6)
    - HomeObjectsTotalStore.Total == 6 después del primer Awake() de HomeInitializer
    - Si se recarga Home sin llamar Clear(), el guard evita sobre-escritura (log no se repite)
    - El componente HomeInitializer está colocado en un GameObject de Home.unity (confirmado en Inspector)
    - read_console sin errores de compilación
  </acceptance_criteria>
  <done>HomeInitializer captura y persiste el total de objetos de Home en HomeObjectsTotalStore.</done>
</task>

<task type="auto">
  <name>Task 2: Modificar CityHomeDoorTrigger para delegar al GameManager</name>
  <files>
    Assets/_Project/Core/SceneManagement/CityHomeDoorTrigger.cs
  </files>
  <read_first>
    - 03-PATTERNS.md §"Modificación: CityHomeDoorTrigger" — patrón exacto: triggered=true solo cuando se confirma transición, fallback a comportamiento anterior si no hay GameManager
    - 03-RESEARCH.md §"Open Items #5 — Triggers de llegada a Home" — código patrón recomendado con FindFirstObjectByType<GameManager>
    - 03-RESEARCH.md §"Pitfall 3" — triggered=true no debe setearse en la primera línea: moverlo al momento de confirmación de transición
    - Assets/_Project/Core/SceneManagement/CityHomeDoorTrigger.cs — código actual completo (líneas 19-32): triggered = true antes del LoadSceneAsync
    - Assets/_Project/Core/SceneManagement/BarDoorTrigger.cs — patrón de trigger análogo (referencia)
    - 03-CONTEXT.md D-04 — la victoria se evalúa al llegar a Home manejando desde City
  </read_first>
  <action>
    Modificar OnTriggerEnter en CityHomeDoorTrigger.cs. El nuevo flujo:
    1. Guard: if (triggered) return;
    2. Guard: if (!other.CompareTag(playerTag)) return;
    3. Guardar posición del auto: var car = FindFirstObjectByType<CarController>(); if (car != null) CarStateStore.Save(car.transform); — igual que antes.
    4. Buscar GameManager: var gm = FindFirstObjectByType<GameManager>();
    5. Si gm != null: setear triggered = true; llamar gm.OnPlayerArrivedHome(); return; — GameManager se encarga de cargar la escena correcta.
    6. Fallback (sin GameManager): setear triggered = true; SceneManager.LoadSceneAsync(sceneToLoad); — comportamiento anterior, para backward compatibility.

    Agregar comentario en español: "D-04: delegar al GameManager la decision de cargar Home o Result/Victory." No agregar using adicionales — CityHomeDoorTrigger ya importa UnityEngine y UnityEngine.SceneManagement.

    CRÍTICO (per Pitfall 3): triggered = true se setea DESPUÉS de las guards de tag/controlado, y ANTES de la llamada al GameManager o al LoadSceneAsync. Si el GameManager devuelve sin cargar escena (caso imposible por diseño actual, pero defensivo), triggered queda true y el trigger no re-dispara. Esto es correcto: el jugador debe retroceder y volver a activar el trigger si la condición no se cumplió — no hay doble-entrada en el mismo frame.
  </action>
  <verify>
    Play en City.unity + escenario A (victoria): usar debug para setear HomeObjectsTotalStore.Total=6, DeliveredObjectsStore.MarkTaken los 6 ids, DrunkLevelStore.Save(6). Conducir el auto hasta el trigger de entrada a Home. Verificar en Console que GameManager.OnPlayerArrivedHome() se llama y SceneManager carga "Result". Verificar que GameResultStore.Result == Victory.

    Play en City.unity + escenario B (sin victoria): no modificar stores (vacíos). Conducir hasta el trigger. Verificar que SceneManager carga "Home" normalmente (no "Result").

    Play en City.unity + escenario C (sin GameManager en escena): quitar el GameObject de GameManager de City. Conducir hasta el trigger. Verificar que el fallback carga "Home" sin errores.
  </verify>
  <acceptance_criteria>
    - CityHomeDoorTrigger.cs compila sin errores
    - Escenario A (condición de victoria): trigger delega a GameManager y carga "Result" con GameResult.Victory
    - Escenario B (sin condición): trigger delega a GameManager y carga "Home"
    - Escenario C (sin GameManager): fallback carga "Home" sin NullReferenceException
    - triggered=true se setea solo cuando se va a cargar una escena (no en la primera línea del handler)
    - read_console sin errores de compilación ni NullReferenceException en ninguno de los tres escenarios
  </acceptance_criteria>
  <done>Slice de victoria end-to-end conectado: llegar a Home manejando con condición cumplida → Result/Victory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna) | Juego local single-player; triggers de Unity son callbacks internos del motor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | (n/a) | HomeInitializer, CityHomeDoorTrigger | accept | Juego offline; sin input externo ni red. El trigger verifica CompareTag("player") antes de actuar — única validación necesaria. |
| T-03-SC | Tampering | npm/pip installs | accept | No se instalan paquetes externos en este plan. |
</threat_model>

<verification>
Al completar los dos tasks:
1. read_console: cero errores de compilación.
2. Play en Home.unity: Console muestra total de objetos (6).
3. Play en City.unity, escenario de victoria (stores seteados por debug): trigger de Home → carga Result con GameResult.Victory.
4. Play en City.unity, escenario normal: trigger de Home → carga Home.
5. GameResultStore.Result permanece None en una partida sin condición de victoria.
</verification>

<success_criteria>
- GAME-02: haber vendido todos los objetos + alcohol >= 6 al llegar a Home → transición a Result/Victory
- GAME-03 (parcial): escena Result se carga con GameResultStore.Result == Victory (pantalla aún vacía — se completa en 03-04)
- Partida normal sin condición de victoria: llegar a Home carga Home (criterio 5 del roadmap)
- Sin regresiones en el flow de derrota implementado en 03-02
</success_criteria>

<output>
Crear .planning/phases/03-loop-de-victoria-y-derrota/03-03-SUMMARY.md cuando completo.
</output>
