---
phase: 01-econom-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/ScriptableObjects/Items/SellableDefinition.cs
  - Assets/_Project/ScriptableObjects/Items/Instances/TV.asset
  - Assets/_Project/ScriptableObjects/Items/Instances/Lampara.asset
  - Assets/_Project/ScriptableObjects/Items/Instances/Cuadro.asset
  - Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs
  - Assets/_Project/Core/SceneManagement/HeldObjectStore.cs
  - Assets/_Project/Gameplay/Items/CarryableObject.cs
  - Assets/_Project/Gameplay/Items/SellCounter.cs
  - Assets/_Project/Gameplay/Player/PlayerPickup.cs
autonomous: true
requirements:
  - ECON-01
  - ECON-02
  - PAT-01

must_haves:
  truths:
    - "El jugador puede agarrar un CarryableObject en Home y el store recuerda qué objeto es"
    - "Al ir al Bar y apretar E mirando el mostrador, el dinero del jugador sube en el valor del objeto"
    - "El dinero persiste al salir del Bar hacia City (PlayerMoneyStore sobrevive a LoadSceneAsync)"
    - "Los CarryableObject tienen SellableDefinition asignada en el Inspector con sellValue configurable"
    - "Instancias del mismo tipo de objeto comparten el mismo asset SellableDefinition (Flyweight)"
  artifacts:
    - path: "Assets/_Project/ScriptableObjects/Items/SellableDefinition.cs"
      provides: "ScriptableObject Flyweight con sellValue + itemName"
      contains: "CreateAssetMenu"
    - path: "Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs"
      provides: "Store estático con Add/Spend/CanAfford/Clear"
      exports: ["PlayerMoneyStore.Add", "PlayerMoneyStore.Spend", "PlayerMoneyStore.CanAfford", "PlayerMoneyStore.Clear"]
    - path: "Assets/_Project/Core/SceneManagement/HeldObjectStore.cs"
      provides: "Store estático que reemplaza hasHeldObject con SellableDefinition + StableId"
      exports: ["HeldObjectStore.SetHeld", "HeldObjectStore.Clear", "HeldObjectStore.HasHeldObject"]
    - path: "Assets/_Project/Gameplay/Items/SellCounter.cs"
      provides: "Componente de mostrador en Bar: raycast + E vende el objeto del HeldObjectStore"
      contains: "RequireComponent"
    - path: "Assets/_Project/Gameplay/Items/CarryableObject.cs"
      provides: "Modificado: campo SellableDefinition + OnPickedUp llama HeldObjectStore.SetHeld"
  key_links:
    - from: "Assets/_Project/Gameplay/Items/CarryableObject.cs"
      to: "Assets/_Project/Core/SceneManagement/HeldObjectStore.cs"
      via: "OnPickedUp() llama HeldObjectStore.SetHeld(definition, StableId)"
      pattern: "HeldObjectStore\\.SetHeld"
    - from: "Assets/_Project/Gameplay/Items/SellCounter.cs"
      to: "Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs"
      via: "TrySell() llama PlayerMoneyStore.Add(def.SellValue)"
      pattern: "PlayerMoneyStore\\.Add"
    - from: "Assets/_Project/Gameplay/Items/SellCounter.cs"
      to: "Assets/_Project/Core/SceneManagement/DeliveredObjectsStore.cs"
      via: "TrySell() llama DeliveredObjectsStore.MarkTaken(HeldObjectStore.HeldObjectId)"
      pattern: "DeliveredObjectsStore\\.MarkTaken"
    - from: "Assets/_Project/Gameplay/Player/PlayerPickup.cs"
      to: "Assets/_Project/Core/SceneManagement/HeldObjectStore.cs"
      via: "HasHeldObject getter y ConsumeHeldObject() delegan a HeldObjectStore"
      pattern: "HeldObjectStore\\.HasHeldObject"
---

<objective>
Entregar el primer slice jugable de la economía: el jugador puede agarrar un objeto de muebles en Home, llevarlo al Bar y venderlo en el mostrador con E para recibir su valor en dinero. El dinero persiste al volver a City.

Purpose: Establece los cimientos del Flyweight (SellableDefinition), los dos stores de persistencia (PlayerMoneyStore, HeldObjectStore) y el componente de venta (SellCounter). Sin esto, el Slice B no tiene dinero que gastar ni mecánica de porteo entre escenas.

Output: SellableDefinition.cs + assets, PlayerMoneyStore.cs, HeldObjectStore.cs, SellCounter.cs, CarryableObject.cs modificado, PlayerPickup.cs modificado (delegación de hasHeldObject), instancias de SellCounter en Bar.unity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-econom-a/01-CONTEXT.md
@.planning/phases/01-econom-a/01-RESEARCH.md
@.planning/phases/01-econom-a/01-PATTERNS.md

<interfaces>
<!-- Contratos y extractos de código extraídos del codebase existente -->
<!-- El ejecutor los usa directamente — no es necesario leer los archivos fuente para estos fragmentos -->

Desde Assets/_Project/Core/SceneManagement/CarStateStore.cs (análogo exacto de store estático):
```csharp
public static class CarStateStore
{
    public static bool HasSavedState { get; private set; }
    public static Vector3 Position { get; private set; }
    public static Quaternion Rotation { get; private set; }

    public static void Save(Transform car)
    {
        if (car == null) return;
        Position = car.position;
        Rotation = car.rotation;
        HasSavedState = true;
    }
    public static void Clear() { HasSavedState = false; }
}
```

Desde Assets/_Project/Core/SceneManagement/DeliveredObjectsStore.cs (patrón HashSet + MarkTaken — usar en SellCounter):
```csharp
public static class DeliveredObjectsStore
{
    static readonly HashSet<string> takenIds = new HashSet<string>();
    public static void MarkTaken(string id) { if (string.IsNullOrEmpty(id)) return; takenIds.Add(id); }
    public static bool IsTaken(string id) { ... }
    public static void Clear() { takenIds.Clear(); }
}
```

Desde Assets/_Project/Gameplay/Items/CarryableObject.cs (campos relevantes actuales):
```csharp
// Líneas 21-36: StableId
[SerializeField] private string objectId = "";
public string StableId { get { /* deriva de objectId o jerarquía */ } }

// Líneas 82-88: OnPickedUp ACTUAL (a modificar — mover MarkTaken a SellCounter)
public void OnPickedUp()
{
    DeliveredObjectsStore.MarkTaken(StableId);   // <-- MOVER a SellCounter.TrySell()
    SetHighlighted(false);
    gameObject.SetActive(false);
}
```

Desde Assets/_Project/Gameplay/Player/PlayerPickup.cs (campos a migrar):
```csharp
// Línea 33 — campo estático a eliminar y reemplazar por HeldObjectStore:
static bool hasHeldObject;
public bool HasHeldObject => hasHeldObject;
public void ConsumeHeldObject() { hasHeldObject = false; }

// Líneas 100-108 — rama de CarryableObject en Update (condición a actualizar):
else if (currentCarryable != null && !hasHeldObject)
{
    PickupCarryable(currentCarryable);
}

// Líneas 203-211 — PickupCarryable (línea hasHeldObject = true a eliminar):
void PickupCarryable(CarryableObject carryable)
{
    if (carryable == null) return;
    carryable.OnPickedUp();
    hasHeldObject = true;          // <- eliminar; lo hace HeldObjectStore.SetHeld()
    currentCarryable = null;
    lastHighlightedCarryable = null;
}
```

Desde Assets/_Project/Gameplay/Player/PlayerPickup.cs — patrón AudioSource + Resources.Load (análogo para SellCounter):
```csharp
// Awake (líneas 64-87):
sfxSource = gameObject.GetComponent<AudioSource>();
if (sfxSource == null) sfxSource = gameObject.AddComponent<AudioSource>();
sfxSource.playOnAwake = false;

if (drinkSipClip == null)
{
    drinkSipClip = Resources.Load<AudioClip>("Audio/SFX/DrinkSip");
    if (drinkSipClip == null) drinkSipClip = Resources.Load<AudioClip>("Audio/DrinkSip");
}
```

Desde Assets/_Project/Core/SceneManagement/BarDoorTrigger.cs (patrón RequireComponent + isTrigger en Reset()):
```csharp
[RequireComponent(typeof(Collider))]
public class BarDoorTrigger : MonoBehaviour
{
    [Header("Config")]
    [SerializeField] private string sceneToLoad = "Bar";
    private bool triggered;
    void Reset() { var col = GetComponent<Collider>(); if (col != null) col.isTrigger = true; }
}
```

Desde Assets/_Project/Core/Managers/DrunkManager.cs (patrón Header + SerializeField private + property =>):
```csharp
[Header("Config")]
[SerializeField] private int maxLevel = 24;
public int MaxLevel => maxLevel;
```
</interfaces>
</context>

## Phase Goal

**As a** jugador, **I want to** vender objetos de mi casa para conseguir dinero, **so that** el loop tenga una economía real en lugar de trueque.

<tasks>

<task type="auto">
  <name>Task 1: SellableDefinition (Flyweight SO) + Stores de persistencia</name>
  <files>
    Assets/_Project/ScriptableObjects/Items/SellableDefinition.cs,
    Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs,
    Assets/_Project/Core/SceneManagement/HeldObjectStore.cs
  </files>

  <read_first>
    - Assets/_Project/Core/SceneManagement/CarStateStore.cs — análogo exacto del store estático (properties + Clear())
    - Assets/_Project/Core/SceneManagement/DeliveredObjectsStore.cs — patrón guard clause en string + HashSet
    - Assets/_Project/Core/Managers/DrunkManager.cs — patrón [Header] + [SerializeField] private + property =>
    - .planning/phases/01-econom-a/01-PATTERNS.md §"PlayerMoneyStore" y §"HeldObjectStore" — extractos completos de código
    - .planning/phases/01-econom-a/01-RESEARCH.md §"Patrón 1" y §"Patrón 2" y §"Patrón 3"
  </read_first>

  <action>
    Crear tres archivos nuevos respetando las convenciones del proyecto (namespace global, sin using redundantes, comentarios en español, guard clauses).

    SellableDefinition.cs — ScriptableObject Flyweight (per D-05, D-06):
    - Atributo [CreateAssetMenu(fileName = "SellableDefinition", menuName = "Drunk Simulator/Sellable Definition")]
    - Hereda de ScriptableObject
    - Campos: [Header("Identidad")] [SerializeField] private string itemName = "Objeto"; [Header("Economia")] [Tooltip("Valor en pesos al vender")] [SerializeField] private int sellValue = 20;
    - Properties de solo lectura: public string ItemName => itemName; public int SellValue => sellValue;
    - Sin namespace, comentarios en español, XML <summary> en español

    PlayerMoneyStore.cs — store estático para el dinero del jugador (per D-11):
    - public static class PlayerMoneyStore (sin herencia, sin namespace)
    - public static int Money { get; private set; } = 0;
    - Add(int amount): guard clause if amount <= 0, suma a Money
    - Spend(int amount): guard clause if amount <= 0 retorna true; if Money < amount retorna false; descuenta y retorna true
    - CanAfford(int amount): expression body Money >= amount
    - Clear(): Money = 0; (para "Nueva partida")
    - Debug.Log con prefijo [PlayerMoneyStore] en Add y Spend
    - XML <summary> en español en cada método

    HeldObjectStore.cs — store estático que reemplaza hasHeldObject de PlayerPickup (per D-03):
    - public static class HeldObjectStore (sin herencia, sin namespace)
    - Propiedades: public static bool HasHeldObject { get; private set; } public static SellableDefinition HeldDefinition { get; private set; } public static string HeldObjectId { get; private set; }
    - SetHeld(SellableDefinition definition, string stableId): guard clause if definition == null return; asignar los tres campos y HasHeldObject = true
    - Clear(): poner los tres en null/false
    - Sin using adicionales (SellableDefinition es del mismo ensamblado)
    - XML <summary> en español

    Luego crear en el Editor de Unity los assets ScriptableObject concretos con [CreateAssetMenu] para los tipos de objetos de Home. Usar la herramienta UnityMCP para verificar compilación antes de continuar. Los assets .asset van en Assets/_Project/ScriptableObjects/Items/Instances/. Crear al menos: TV.asset (sellValue = 50), Lampara.asset (sellValue = 30), Cuadro.asset (sellValue = 20). Los valores exactos son tuneable por Inspector (per D-09).
  </action>

  <verify>
    <automated>Verificación de source: grep -c "CreateAssetMenu" "Assets/_Project/ScriptableObjects/Items/SellableDefinition.cs" debe retornar 1. grep -c "public static int Money" "Assets/_Project/Core/SceneManagement/PlayerMoneyStore.cs" debe retornar 1. grep -c "HeldDefinition" "Assets/_Project/Core/SceneManagement/HeldObjectStore.cs" debe retornar 1.</automated>
    <human-check>En el Unity Editor: verificar que el menú Assets > Create > Drunk Simulator > Sellable Definition está disponible y que los assets TV.asset, Lampara.asset, Cuadro.asset existen en Assets/_Project/ScriptableObjects/Items/Instances/ con sus sellValue configurados.</human-check>
  </verify>

  <acceptance_criteria>
    - SellableDefinition.cs compila sin errores (read_console en Unity no muestra errores de compilación)
    - PlayerMoneyStore.cs compila; PlayerMoneyStore.Money empieza en 0; Add(50) lo deja en 50; Spend(30) retorna true y lo deja en 20; Spend(100) retorna false y lo deja en 20; Clear() lo deja en 0
    - HeldObjectStore.cs compila; SetHeld con null no modifica el estado; SetHeld con asset válido activa HasHeldObject; Clear() deja HasHeldObject=false y HeldDefinition=null
    - Assets TV.asset, Lampara.asset, Cuadro.asset existen con sellValue > 0
    - Ningún campo usa "namespace" — verificar con grep
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CarryableObject + SellCounter + cableado en Inspector (Home + Bar)</name>
  <files>
    Assets/_Project/Gameplay/Items/CarryableObject.cs,
    Assets/_Project/Gameplay/Items/SellCounter.cs,
    Assets/_Project/Gameplay/Player/PlayerPickup.cs
  </files>

  <read_first>
    - Assets/_Project/Gameplay/Items/CarryableObject.cs — leer completo; especialmente OnPickedUp (líneas 82-88) y StableId
    - Assets/_Project/Gameplay/Player/PlayerPickup.cs — leer completo; especialmente el campo static bool hasHeldObject, HasHeldObject, ConsumeHeldObject() y la rama PickupCarryable en Update (líneas 100-108) y el método PickupCarryable (203-211)
    - Assets/_Project/Core/SceneManagement/BarDoorTrigger.cs — patrón RequireComponent + Header + SerializeField + Input.GetKeyDown
    - .planning/phases/01-econom-a/01-PATTERNS.md §"CarryableObject.cs — modificaciones" y §"SellCounter.cs"
    - .planning/phases/01-econom-a/01-RESEARCH.md §"Patrón 4: SellCounter" y §"Pitfall 3: hasHeldObject" y §"Open Questions"
    - .planning/phases/01-econom-a/01-CONTEXT.md §D-01 y §D-02 y §D-03
  </read_first>

  <action>
    PASO A — Modificar CarryableObject.cs:
    Agregar campo nuevo antes de los métodos existentes: [Header("Definicion")] [SerializeField] private SellableDefinition definition; con property public SellableDefinition Definition => definition; y public int SellValue => definition != null ? definition.SellValue : 0;
    En OnPickedUp(): ELIMINAR la línea DeliveredObjectsStore.MarkTaken(StableId) — esa llamada se mueve a SellCounter.TrySell() porque el objeto se "entrega" al vender, no al agarrar. Reemplazarla por HeldObjectStore.SetHeld(definition, StableId). Mantener SetHighlighted(false) y gameObject.SetActive(false).

    PASO B — Modificar PlayerPickup.cs para delegar hasHeldObject a HeldObjectStore:
    Eliminar el campo "static bool hasHeldObject;" (línea ~33). Cambiar la property HasHeldObject para que retorne HeldObjectStore.HasHeldObject. Cambiar ConsumeHeldObject() para que llame HeldObjectStore.Clear(). En la condición de Update que verifica !hasHeldObject (línea ~105), reemplazar por !HeldObjectStore.HasHeldObject. En PickupCarryable(), eliminar la línea "hasHeldObject = true;" (ya lo hace HeldObjectStore.SetHeld dentro de CarryableObject.OnPickedUp). No cambiar ninguna otra lógica de PlayerPickup en este plan.

    PASO C — Crear SellCounter.cs en Assets/_Project/Gameplay/Items/:
    - [RequireComponent(typeof(Collider))]
    - Atributos de Inspector: [Header("Audio")] [SerializeField] private AudioClip sellClip; [SerializeField, Range(0f, 1f)] private float sellVolume = 1f; [Header("Config")] [SerializeField] public KeyCode interactKey = KeyCode.E;
    - Campo privado: AudioSource sfxSource; y bool playerInRange; (controla si el raycast del jugador apunta al mostrador)
    - Awake(): resolver sfxSource con GetComponent + AddComponent si null, playOnAwake = false; cargar sellClip con Resources.Load("Audio/SFX/Sell"), fallback a Resources.Load("Audio/SFX/PayDrink")
    - Implementar IInteractable implícito: el SellCounter detecta la tecla E en su propio Update SOLO cuando HeldObjectStore.HasHeldObject == true. Para integrarse con PlayerPickup sin duplicar raycast: extender PlayerPickup.UpdateSelectionByLook para que también busque SellCounter por GetComponentInParent igual que busca PickupItem y CarryableObject — agregar campo currentSellCounter en PlayerPickup, detectarlo en el raycast, y en Update llamar currentSellCounter?.OnPlayerInteract() cuando se aprieta E. Alternativamente (más simple para MVP), SellCounter puede tener su propio raycast con OnTriggerStay/OnTriggerEnter detectando el tag del jugador + raycast de confirmación — elegir la opción que cause menos cambios en PlayerPickup. La opción recomendada por RESEARCH.md es extender PlayerPickup (Opción A); seguir esa guía.
    - Método TrySell(): guard clause if !HeldObjectStore.HasHeldObject return; leer def = HeldObjectStore.HeldDefinition; guard if def == null return; int valor = def.SellValue; PlayerMoneyStore.Add(valor); if !string.IsNullOrEmpty(HeldObjectStore.HeldObjectId) → DeliveredObjectsStore.MarkTaken(HeldObjectStore.HeldObjectId); HeldObjectStore.Clear(); PlayOneShot(sellClip, sellVolume) con guard null; Debug.Log("[SellCounter] Objeto vendido por $X. Saldo: $Y")

    PASO D — Cableado en Inspector y escenas (via Unity MCP):
    1. En Home.unity: seleccionar cada CarryableObject y asignarle la SellableDefinition correspondiente (TV → TV.asset, etc.). Asignar también un objectId único a cada uno (per CONCERNS.md sobre colisión de StableId) — usar strings simples como "home-obj-1", "home-obj-2", etc.
    2. En Bar.unity: crear un GameObject "MostradorVenta" con Box Collider y componente SellCounter. Posicionar en la barra del Bar (usar coordenadas razonables o duplicar desde un objeto existente de la escena). Asegurarse de que el layer del Collider esté en el pickupLayerMask de PlayerPickup.
    3. Verificar compilación con read_console antes de abrir Play Mode.

    NOTAS DE CONVENCIONES:
    - Input legacy: Input.GetKeyDown(interactKey) en Update — no Input System
    - Namespace global (sin namespace)
    - Comentarios en español
    - Guard clauses, no excepciones
  </action>

  <verify>
    <automated>
      grep -c "HeldObjectStore.SetHeld" "Assets/_Project/Gameplay/Items/CarryableObject.cs" debe retornar 1.
      grep -c "DeliveredObjectsStore.MarkTaken" "Assets/_Project/Gameplay/Items/CarryableObject.cs" debe retornar 0 (se movió a SellCounter).
      grep -c "static bool hasHeldObject" "Assets/_Project/Gameplay/Player/PlayerPickup.cs" debe retornar 0.
      grep -c "HeldObjectStore" "Assets/_Project/Gameplay/Player/PlayerPickup.cs" debe retornar mayor que 0.
      grep -c "TrySell" "Assets/_Project/Gameplay/Items/SellCounter.cs" debe retornar 1.
    </automated>
    <human-check>
      Play Mode — Loop de venta completo:
      1. Abrir Home.unity en Play Mode. Verificar que los CarryableObjects tienen SellableDefinition asignada (seleccionarlos en el Inspector durante Play Mode).
      2. Agarrar un objeto con E (PlayerPickup). Verificar que HeldObjectStore.HasHeldObject = true (Debug.Log o Inspector).
      3. Transitar a Bar (por la puerta). Verificar que HeldObjectStore.HasHeldObject sigue siendo true tras la carga de escena.
      4. Mirar el MostradorVenta y apretar E. Verificar que Debug.Log "[SellCounter] Objeto vendido por $X" aparece en la consola.
      5. Verificar PlayerMoneyStore.Money > 0 (via Debug.Log en SellCounter ya impreso).
      6. Salir al City. Verificar que PlayerMoneyStore.Money sigue siendo el mismo valor (persiste entre escenas).
    </human-check>
  </verify>

  <acceptance_criteria>
    - CarryableObject.cs compila: campo definition de tipo SellableDefinition presente; OnPickedUp llama HeldObjectStore.SetHeld y NO llama DeliveredObjectsStore.MarkTaken (eso está en SellCounter)
    - PlayerPickup.cs compila: "static bool hasHeldObject" eliminado; HasHeldObject retorna HeldObjectStore.HasHeldObject; ConsumeHeldObject() llama HeldObjectStore.Clear()
    - SellCounter.cs compila con RequireComponent(typeof(Collider)); TrySell llama PlayerMoneyStore.Add y DeliveredObjectsStore.MarkTaken y HeldObjectStore.Clear
    - En Play Mode: agarrar objeto en Home → cruzar al Bar → apretar E en mostrador → Debug.Log muestra monto vendido → PlayerMoneyStore.Money > 0 → cruzar a City → PlayerMoneyStore.Money sigue igual (no es 0)
    - Cada CarryableObject en Home.unity tiene SellableDefinition asignada con sellValue > 0 y objectId único no vacío
    - El MostradorVenta existe en Bar.unity con SellCounter y BoxCollider
    - Con $0 (inicio de partida) antes de vender, PlayerMoneyStore.Money == 0 (per D-07)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unity Editor / Play Mode | Único entorno de ejecución — juego offline, single-player, sin red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | PlayerMoneyStore (static int Money) | accept | Juego offline single-player; el dinero vive solo en memoria de proceso; no hay superficie de ataque. Sin red, sin disco, sin entrada de usuario no confiable. |
| T-01-02 | Tampering | HeldObjectStore (static SellableDefinition) | accept | Misma razón: juego local sin persistencia a disco. Un cheater que modifique memoria en un juego académico offline no es una amenaza relevante. |
| T-01-03 | Denial of Service | SellCounter.TrySell() + PlayerMoneyStore.Add() | mitigate | Guard clauses en TrySell (if !HasHeldObject return; if def == null return;) previenen ejecución duplicada o con estado nulo. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Esta fase no instala ningún package externo. No aplica Package Legitimacy Gate. |

**Conclusión:** No hay superficie de ataque de severidad high. El juego es offline, single-player, sin endpoints de red, sin deserialización de datos externos y sin autenticación. El threat model cumple el gate ASVS L1 por ausencia de vectores de ataque relevantes, no por ignorarlos.
</threat_model>

<verification>
Verificación global del Slice A antes de avanzar al Plan 02:

1. Compilación limpia: read_console no muestra errores ni warnings de los scripts nuevos/modificados.
2. Loop de venta funciona de punta a punta en Play Mode (descrito en human-check de Task 2).
3. grep -v "^//" Assets/_Project/Gameplay/Player/PlayerPickup.cs | grep -c "static bool hasHeldObject" == 0
4. grep -c "SellableDefinition" Assets/_Project/Gameplay/Items/CarryableObject.cs >= 1
5. PlayerMoneyStore.Money == 0 al inicio; sube al vender; persiste entre escenas.
6. Dos CarryableObjects del mismo tipo (si los hay) comparten el mismo asset SellableDefinition — verificar en Inspector que es el mismo objeto, no una copia.
</verification>

<success_criteria>
- ECON-01: Cada CarryableObject en Home.unity tiene sellValue configurable vía SellableDefinition asignada en Inspector.
- ECON-02: Vender en el mostrador del Bar acredita dinero al jugador; el dinero persiste al volver a City.
- PAT-01 (parcial): SellableDefinition es ScriptableObject compartido; instancias del mismo tipo de objeto apuntan al mismo asset (Flyweight de estado intrínseco).
- HeldObjectStore reemplaza static bool hasHeldObject sin coexistencia de ambos.
</success_criteria>

<output>
Crear `.planning/phases/01-econom-a/01-01-SUMMARY.md` al completar este plan.
</output>
