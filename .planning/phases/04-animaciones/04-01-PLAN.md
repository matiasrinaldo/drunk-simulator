---
phase: 04-animaciones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/UI/HUD/HUDController.cs
  - Assets/_Project/Gameplay/Player/PlayerPickup.cs
autonomous: false
requirements: [ANIM-02]
must_haves:
  truths:
    - "Sobrio (EffectIntensity <= 0.05): la DrunkBar esta perfectamente quieta (localScale == (1,1,1))"
    - "Tras varios sorbos la DrunkBar late visiblemente; el pulso es mas fuerte y rapido a medida que sube EffectIntensity"
    - "El pulso NO corrompe el fill: el nivel sigue lerpeando correctamente (pulso sobre DrunkBar.localScale, no sobre Fill/fillAmount)"
    - "Intentar comprar una bebida sin dinero suficiente hace que MoneyText tiemble ~0.30s y vuelva a su posicion y color originales"
    - "Tras rechazos repetidos MoneyText vuelve exactamente a anchoredPosition (0,28) en blanco (sin drift)"
    - "Ambas animaciones son codigo puro (sin ningun AnimationClip)"
  artifacts:
    - path: "Assets/_Project/UI/HUD/HUDController.cs"
      provides: "Pulso senoidal de DrunkBar.localScale por EffectIntensity + FlashMoneyRejected/StartMoneyWobble (wobble one-shot)"
      contains: "FlashMoneyRejected"
    - path: "Assets/_Project/Gameplay/Player/PlayerPickup.cs"
      provides: "Llamada a HUDController.FlashMoneyRejected() en la rama !CanAfford"
      contains: "HUDController.FlashMoneyRejected"
  key_links:
    - from: "Assets/_Project/Gameplay/Player/PlayerPickup.cs"
      to: "HUDController.FlashMoneyRejected"
      via: "llamada estatica en la rama !CanAfford (mismo momento que el SFX de rechazo)"
      pattern: "HUDController\\.FlashMoneyRejected"
    - from: "Assets/_Project/UI/HUD/HUDController.cs"
      to: "DrunkManager.EffectIntensity"
      via: "lectura por frame en Update (campo drunkManager ya cacheado en OnSceneLoaded)"
      pattern: "drunkManager.*EffectIntensity"
---

<objective>
Slice ANIM-02 end-to-end: agregar las dos animaciones por codigo (sin AnimationClip) sobre el HUD existente, segun el Motion Contract del UI-SPEC.

1. **Pulso de la barra de borrachera (Token A):** la DrunkBar "respira" (throb de escala) con amplitud/frecuencia escaladas por `DrunkManager.EffectIntensity`. Mas borracho => pulso mas fuerte y rapido.
2. **Wobble del texto de dinero (Token B):** MoneyText tiembla brevemente cuando se rechaza una compra por falta de dinero (rama `!CanAfford` ya existente en PlayerPickup), via un one-shot `HUDController.FlashMoneyRejected()`.

Purpose: Satisface ANIM-02 (animacion por codigo en al menos un elemento) y el criterio 2/3 de exito de la fase. Refuerza el tema del juego (mas borracho => HUD mas inestable).
Output: Modificaciones a HUDController.cs (pulso + wobble) y PlayerPickup.cs (disparo del wobble).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-animaciones/04-UI-SPEC.md
@.planning/phases/04-animaciones/04-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Contratos clave que el ejecutor necesita. Extraidos del codigo. No explorar el codebase. -->

HUDController.cs (Assets/_Project/UI/HUD/HUDController.cs) — singleton DontDestroyOnLoad construido BeforeSceneLoad:
- Campo ya existente: `private DrunkManager drunkManager;` re-bindeado cada carga de escena en OnSceneLoaded (drunkManager = FindFirstObjectByType<DrunkManager>()). REUSAR ese campo, NO agregar un segundo cache.
- Campo ya existente: `private Image fillImage;` y `private TextMeshProUGUI moneyText;`
- En BuildHUD() existe el local `RectTransform barRect` (GameObject "DrunkBar", sizeDelta 220x20, pivot (0,0), anchoredPosition Vector2.zero). El ejecutor debe capturar barRect en un nuevo campo privado para escalarlo en Update.
- moneyRect (RectTransform de "MoneyText") se construye con anchoredPosition = new Vector2(0f, 28f), pivot (0,0), color blanco.
- Update() existente lerpea fillImage.fillAmount hacia targetFillAmount (Mathf.Lerp(..., 3f * Time.deltaTime)) con snapping <0.001. NO tocar esa logica.
- static SetVisible(bool) ya existe: `public static void SetVisible(bool v) { if (instance != null) instance.gameObject.SetActive(v); }` — usar como molde para el accessor estatico nuevo.
- jerarquia verificada: HUDGroup -> DrunkBar -> Track -> Fill.

DrunkManager: expone `public float EffectIntensity { get; }` (escalar 0..1 no lineal).

PlayerPickup.cs (Assets/_Project/Gameplay/Player/PlayerPickup.cs) — rama de rechazo ya existente en Update (bloque pickupKey):
```
int precio = currentPickupItem.Price;
if (!PlayerMoneyStore.CanAfford(precio))
{
    // SFX de rechazo si no hay plata suficiente (D-10)
    if (rejectClip != null && sfxSource != null)
        sfxSource.PlayOneShot(rejectClip, Mathf.Clamp01(rejectVolume));
    return;
}
```
La senal del wobble va dentro de este bloque, antes del return, mismo momento que el SFX.

Analog del coroutine one-shot con snap final garantizado: PlayerPickup.AnimateHeldDrinkPose (PlayerPickup.cs:330-355) — patron elapsed/duration con smoothstep y snap final; y el patron de handle unico de coroutine (drinkAnimationRoutine + StopDrinkAnimation, PlayerPickup.cs:48,385-394).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pulso de DrunkBar por EffectIntensity (Token A)</name>
  <files>Assets/_Project/UI/HUD/HUDController.cs</files>
  <read_first>
    - Assets/_Project/UI/HUD/HUDController.cs (archivo a modificar: BuildHUD, Update, OnSceneLoaded — donde se bindea drunkManager)
    - Assets/_Project/Core/Managers/MouseLook.cs (analog canonico del consumidor de EffectIntensity: cache en Awake + FindFirstObjectByType fallback, lee EffectIntensity por frame, multiplica seno por amplitud)
    - .planning/phases/04-animaciones/04-UI-SPEC.md (Motion Token A + Landmine pulso-vs-fill, lineas 82-128)
  </read_first>
  <action>
    En BuildHUD(), capturar el RectTransform de la DrunkBar (el local barRect) en un nuevo campo privado, p.ej. `private RectTransform drunkBarRect;`. Como el pivot de DrunkBar es (0,0) (esquina), el throb debe leerse CENTRADO: el ejecutor inserta un wrapper hijo centrado (o re-parenta Track bajo un contenedor con pivot (0.5,0.5)) o escala alrededor del centro; el resultado visible debe ser un respiro simetrico, no un estiramiento anclado a la esquina (UI-SPEC "Pivot for scale", linea 87).

    En Update(), DESPUES de la logica de fill existente (NO tocar el lerp de fillImage.fillAmount), agregar el pulso sobre `drunkBarRect.localScale`:
    - leer la intensidad por frame del campo ya cacheado: `float k = drunkManager != null ? drunkManager.EffectIntensity : 0f;`
    - umbral de activacion: si `k <= 0.05f`, setear `drunkBarRect.localScale = Vector3.one` exactamente y no aplicar pulso (sin jitter en reposo).
    - amplitud lerpeada: `float amp = Mathf.Lerp(0f, 0.08f, k);`
    - frecuencia lerpeada (Hz): `float f = Mathf.Lerp(0f, 1.8f, k);` y convertir a angular con `Time.time * f * 2f * Mathf.PI`.
    - escala uniforme XY: `float s = 1f + Mathf.Sin(Time.time * f * 2f * Mathf.PI) * amp;` y `drunkBarRect.localScale = new Vector3(s, s, 1f);` (Z intacto).

    LANDMINE (UI-SPEC lineas 112-128): el pulso opera sobre DrunkBar.localScale (transform del contenedor), NUNCA sobre fillImage.fillAmount ni escalando solo el hijo Fill. El lerp de fill y el pulso escriben propiedades distintas en objetos distintos => no pueden pelear. Reusar el campo drunkManager existente (re-bindeado en OnSceneLoaded), NO agregar un segundo cache. Comentarios en espanol, namespace global.
  </action>
  <verify>
    <automated>MISSING — proyecto Unity sin framework de tests/CLI. Verificacion: compilacion limpia (consola de Unity sin errores via UnityMCP read_console) + Play Mode manual abajo.</automated>
    <human-check>En Play Mode (escena City o Bar): (1) recien cargada, sin tomar, la barra esta perfectamente quieta (localScale (1,1,1)). (2) Tras varios sorbos la barra late visiblemente y el latido es mas fuerte/rapido a mayor borrachera. (3) El relleno sigue subiendo/lerpeando correctamente mientras la barra late.</human-check>
  </verify>
  <acceptance_criteria>
    - HUDController.cs contiene un campo RectTransform para la DrunkBar capturado en BuildHUD.
    - HUDController.Update aplica drunkBarRect.localScale a partir de Mathf.Sin escalado por drunkManager.EffectIntensity con amp lerp 0->0.08 y f lerp 0->1.8.
    - Con EffectIntensity <= 0.05 el codigo fuerza drunkBarRect.localScale = Vector3.one.
    - HUDController.Update NO modifica la formula existente de fillImage.fillAmount.
    - Consola de Unity sin errores de compilacion.
    - En Play Mode: sobrio => barra quieta; borracho => barra late mas fuerte y rapido; el fill sigue lerpeando bien.
  </acceptance_criteria>
  <done>El pulso de la DrunkBar es visible en Play Mode, escala con EffectIntensity, es inerte cuando sobrio, y no corrompe el fill.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wobble de MoneyText one-shot + disparo desde PlayerPickup (Token B)</name>
  <files>Assets/_Project/UI/HUD/HUDController.cs, Assets/_Project/Gameplay/Player/PlayerPickup.cs</files>
  <read_first>
    - Assets/_Project/UI/HUD/HUDController.cs (BuildHUD: moneyRect anchoredPosition (0,28), moneyText color blanco; SetVisible como molde de accessor estatico)
    - Assets/_Project/Gameplay/Player/PlayerPickup.cs (rama !CanAfford en Update donde va la senal; AnimateHeldDrinkPose L330-355 como molde de coroutine one-shot con snap final; drinkAnimationRoutine + StopDrinkAnimation L48,385-394 como molde de handle unico)
    - .planning/phases/04-animaciones/04-UI-SPEC.md (Motion Token B, lineas 95-108)
  </read_first>
  <action>
    En HUDController.cs agregar el wobble one-shot:
    - un accessor estatico que reproduce el molde de SetVisible: `public static void FlashMoneyRejected() { if (instance != null) instance.StartMoneyWobble(); }`.
    - un metodo de instancia `StartMoneyWobble()` que mantiene UN solo handle de coroutine (campo `private Coroutine moneyWobbleRoutine;`): si ya hay una corriendo, StopCoroutine y re-snapear la base ANTES de reiniciar (no apilar offsets — UI-SPEC "Re-trigger: restart, do not stack offsets"). Guard: si moneyText == null, return.
    - el coroutine corre `duration = 0.30f` escribiendo solo el eje X de anchoredPosition: `offsetX = Mathf.Sin(elapsed * 12f * 2f * Mathf.PI) * 6f * (1f - elapsed/duration)` (Token B: 6px inicial, 12 Hz, envolvente de decaimiento lineal/ease-out). Base X = 0 (la posicion construida es (0,28)). Escribir `moneyText.rectTransform.anchoredPosition = new Vector2(baseX + offsetX, 28f)` cada frame.
    - snap final garantizado (espejo de AnimateHeldDrinkPose L353-354): al terminar, `moneyText.rectTransform.anchoredPosition = new Vector2(0f, 28f)` y `moneyText.color = Color.white` (sin drift tras rechazos repetidos).
    - flash de color OPCIONAL: tinte blanco->#D64545 (rojo destructivo)->blanco sobre los mismos 0.30s; si se usa, volver EXACTAMENTE a Color.white. No reusar ese rojo en ningun otro lado.
    - usar moneyText.rectTransform (UI Overlay => anchoredPosition, nunca world position — RESEARCH Pitfall 5).

    En PlayerPickup.cs, dentro de la rama `if (!PlayerMoneyStore.CanAfford(precio))`, antes del `return;` y junto al SFX de rechazo, agregar una linea: `HUDController.FlashMoneyRejected();`. NO inventar un store persistente nuevo — es un one-shot transitorio.

    Comentarios en espanol, namespace global.
  </action>
  <verify>
    <automated>MISSING — proyecto Unity sin framework de tests/CLI. Verificacion: compilacion limpia (consola de Unity sin errores) + Play Mode manual abajo.</automated>
    <human-check>En Play Mode: acercarse a una bebida sin tener dinero suficiente y pulsar la tecla de pickup => MoneyText tiembla ~0.30s y vuelve a su posicion/color, coincidiendo con el SFX de rechazo existente. Repetir varias veces: el texto vuelve siempre a su lugar exacto (sin drift acumulado).</human-check>
  </verify>
  <acceptance_criteria>
    - HUDController.cs contiene FlashMoneyRejected( y un coroutine de wobble con handle unico (moneyWobbleRoutine).
    - El coroutine escribe moneyText.rectTransform.anchoredPosition.x con Mathf.Sin a 12 Hz, amplitud inicial 6px, envolvente (1 - elapsed/duration), duracion 0.30s.
    - Al terminar resetea anchoredPosition a (0,28) y color a Color.white.
    - Re-disparar mientras corre reinicia el coroutine sin apilar offsets.
    - PlayerPickup.cs contiene HUDController.FlashMoneyRejected( dentro de la rama !CanAfford, antes del return.
    - Consola de Unity sin errores de compilacion.
    - En Play Mode: rechazo de compra => MoneyText tiembla y vuelve a (0,28) blanco; tras repeticiones, sin drift.
  </acceptance_criteria>
  <done>Rechazar una compra por falta de dinero dispara el wobble visible de MoneyText, que vuelve siempre a su posicion/color base sin drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguno) | Juego single-player offline de escritorio: sin red, sin cuentas, sin PII, sin entrada no confiable mas alla de teclado/mouse local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | (n/a) | HUDController / PlayerPickup | accept | Juego offline single-player; sin red/auth/PII/entrada no confiable. No hay amenazas aplicables a ASVS L1. |

> Sin instalaciones de paquetes (todo built-in de Unity 6 / URP) => no aplica Package Legitimacy Audit ni checkpoint T-04-SC.
</threat_model>

<verification>
- Compilacion limpia: consola de Unity sin errores tras editar los dos scripts (UnityMCP read_console o Editor).
- Play Mode (escena City/Bar): pulso de DrunkBar visible y escalado por borrachera; inerte cuando sobrio; fill sin corromper.
- Play Mode: wobble de MoneyText al rechazar compra; vuelve a (0,28) blanco sin drift.
- Ambas animaciones son codigo puro: ningun AnimationClip referenciado (ANIM-02 satisfecho).
</verification>

<success_criteria>
- ANIM-02 satisfecho: al menos un elemento (DrunkBar y MoneyText) se anima solo por codigo modificando RectTransform en Update/coroutine, sin ningun AnimationClip.
- Acceptance Checks 1-6 del UI-SPEC (lineas 220-228) pasan en Play Mode.
</success_criteria>

<output>
Crear `.planning/phases/04-animaciones/04-01-SUMMARY.md` al completar.
</output>
