---
phase: 04-animaciones
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/_Project/Editor/DoorAnimatorBuilder.cs
  - Assets/_Project/Animations/Door.controller
  - Assets/_Project/Animations/DoorOpen.anim
autonomous: true
requirements: [ANIM-01]
must_haves:
  truths:
    - "Existe un AnimatorController Door.controller con dos estados (Closed default + Open) y una transicion Closed->Open con condicion sobre el parametro Open"
    - "Existe un AnimationClip DoorOpen.anim que rota la bisagra de 0 a ~95 grados en ~0.5s con ease-out"
    - "El estado Open referencia el clip DoorOpen; el estado Closed es el default"
    - "El controller y el clip son assets normales (incluibles en build); el generador vive bajo Editor/ y no se referencia desde runtime"
  artifacts:
    - path: "Assets/_Project/Editor/DoorAnimatorBuilder.cs"
      provides: "Herramienta de editor [MenuItem] que genera Door.controller + DoorOpen.anim idempotentemente"
      contains: "MenuItem"
    - path: "Assets/_Project/Animations/Door.controller"
      provides: "AnimatorController con estados Closed/Open + transicion condicional por parametro Open"
      contains: "Open"
    - path: "Assets/_Project/Animations/DoorOpen.anim"
      provides: "Clip de rotacion de bisagra 0->~95 grados ease-out"
  key_links:
    - from: "Assets/_Project/Animations/Door.controller"
      to: "Assets/_Project/Animations/DoorOpen.anim"
      via: "state.motion del estado Open apunta al clip"
      pattern: "DoorOpen"
---

<objective>
Wave 0 de ANIM-01: crear los assets de Animator que el trigger runtime de la Wave 2 va a referenciar. NO hay ningun `.controller`/`.anim` en el proyecto fuera de ThirdParty, asi que esto es trabajo nuevo sin analog runtime.

Se genera, via una herramienta de editor `[MenuItem]` (analog: CityBuilder.cs), un `Door.controller` con estados Closed (default) y Open + una transicion condicional por el parametro `Open`, y un `DoorOpen.anim` que rota la bisagra de 0 a ~95 grados en ~0.5s con ease-out.

Purpose: ANIM-01 exige un Animator/state machine real (>=2 estados + transicion condicional). Estos assets son el prerequisito bloqueante del enganche runtime (Plan 03). `UnityEditor.Animations` y `AnimationClip.SetCurve` son Editor-only, por eso el generador DEBE vivir bajo Editor/ y NO referenciarse desde runtime (RESEARCH Pitfall 2).
Output: DoorAnimatorBuilder.cs (Editor/) + Door.controller + DoorOpen.anim (Animations/).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-animaciones/04-RESEARCH.md
@.planning/phases/04-animaciones/04-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- API Editor-only para generar los assets. Fuente: RESEARCH Code Examples (lineas 247-265). NO explorar el codebase para esto: no hay analog. -->

UnityEditor.Animations.AnimatorController (Editor-only):
- `AnimatorController.CreateAnimatorControllerAtPath("Assets/_Project/Animations/Door.controller")`
- `controller.AddParameter("Open", AnimatorControllerParameterType.Trigger)`
- `var sm = controller.layers[0].stateMachine;`
- `var closed = sm.AddState("Closed");`  // estado default
- `var open = sm.AddState("Open");`
- `open.motion = doorOpenClip;`
- `var t = closed.AddTransition(open); t.AddCondition(AnimatorConditionMode.If, 0f, "Open"); t.hasExitTime = false;`
- `sm.defaultState = closed;`

AnimationClip (Editor-only para SetCurve no-legacy):
- `var clip = new AnimationClip();`
- `clip.SetCurve("", typeof(Transform), "localEulerAngles.y", curve);` con una AnimationCurve de 0 a ~95 grados en ~0.5s ease-out.
- `AssetDatabase.CreateAsset(clip, "Assets/_Project/Animations/DoorOpen.anim");`

Analog para asset-gen idempotente (CityBuilder.cs):
- header Editor-only: `using UnityEditor; using UnityEngine;` y clase `public static class` en namespace global.
- `[MenuItem("Drunk Simulator/Build Door Animator")]` sobre un metodo static.
- folder guard: `if (!AssetDatabase.IsValidFolder("Assets/_Project/Animations")) AssetDatabase.CreateFolder("Assets/_Project", "Animations");`
- load-or-create idempotente: `AssetDatabase.LoadAssetAtPath<...>(path)` y solo crear si null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DoorAnimatorBuilder (Editor) genera Door.controller + DoorOpen.anim</name>
  <files>Assets/_Project/Editor/DoorAnimatorBuilder.cs, Assets/_Project/Animations/Door.controller, Assets/_Project/Animations/DoorOpen.anim</files>
  <read_first>
    - Assets/_Project/Editor/CityBuilder.cs (analog de herramienta de editor: header Editor-only, [MenuItem], AssetDatabase.IsValidFolder/CreateFolder, load-or-create idempotente, namespace global, clase static)
    - .planning/phases/04-animaciones/04-RESEARCH.md (Code Examples lineas 247-265 para el controller; lineas 94-98,141 para el clip; Pitfall 2 sobre Editor-only)
    - .planning/phases/04-animaciones/04-PATTERNS.md (seccion "Door.controller + DoorOpen.anim", lineas 166-207: rutas de creacion y analog CityBuilder)
  </read_first>
  <action>
    Crear `Assets/_Project/Editor/DoorAnimatorBuilder.cs`: una `public static class DoorAnimatorBuilder` en namespace global, header `using UnityEditor; using UnityEditor.Animations; using UnityEngine;`, con `[MenuItem("Drunk Simulator/Build Door Animator")]` sobre un metodo static `Build()`. Espejar la idempotencia de CityBuilder.

    Pasos del Build():
    1. Folder guard: si no existe `Assets/_Project/Animations`, crearlo con AssetDatabase.CreateFolder("Assets/_Project", "Animations").
    2. Crear el clip DoorOpen: `new AnimationClip()`, construir una AnimationCurve que rote la bisagra de 0 a ~95 grados sobre `localEulerAngles.y` en ~0.5s con ease-out (usar AnimationCurve con tangentes suavizadas, p.ej. keyframes (0, 0) y (0.5, 95) con SmoothTangents o EaseInOut). `clip.SetCurve("", typeof(Transform), "localEulerAngles.y", curve);`. Guardar via AssetDatabase.CreateAsset en `Assets/_Project/Animations/DoorOpen.anim` (load-or-create idempotente: si ya existe el asset en esa ruta, no duplicar).
    3. Crear el controller: `AnimatorController.CreateAnimatorControllerAtPath("Assets/_Project/Animations/Door.controller")` (si ya existe, cargar en vez de recrear). `AddParameter("Open", AnimatorControllerParameterType.Trigger)`. Tomar `controller.layers[0].stateMachine`, `AddState("Closed")` (default via `sm.defaultState = closed`) y `AddState("Open")` con `open.motion = clipDoorOpen`. Crear `closed.AddTransition(open)` con `t.AddCondition(AnimatorConditionMode.If, 0f, "Open")` y `t.hasExitTime = false`.
    4. `AssetDatabase.SaveAssets(); AssetDatabase.Refresh();`.

    CRITICO (RESEARCH Pitfall 2): este archivo usa `UnityEditor.Animations` y `SetCurve` (Editor-only) => DEBE vivir bajo `Assets/_Project/Editor/` y NUNCA ser referenciado desde codigo runtime. El Door.controller y el DoorOpen.anim SON assets normales y SI se incluyen en build.

    Tras crear el script, el ejecutor debe correr el menu **Drunk Simulator -> Build Door Animator** en el Editor (o via UnityMCP) para materializar los dos assets, y chequear read_console sin errores. Comentarios en espanol.
  </action>
  <verify>
    <automated>MISSING — proyecto Unity sin framework de tests/CLI. Verificacion: compilacion limpia del script Editor (read_console sin errores) y existencia de los dos assets tras correr el menu.</automated>
    <human-check>Correr el menu "Drunk Simulator -> Build Door Animator". Abrir el Animator window con Door.controller seleccionado: ver dos estados (Closed default + Open) y una flecha de transicion Closed->Open con condicion "Open". Seleccionar DoorOpen.anim y confirmar que anima la rotacion Y de 0 a ~95 grados.</human-check>
  </verify>
  <acceptance_criteria>
    - Assets/_Project/Editor/DoorAnimatorBuilder.cs existe, contiene MenuItem("Drunk Simulator/Build Door Animator") y usa UnityEditor.Animations.
    - Tras correr el menu, existe Assets/_Project/Animations/Door.controller con estados Closed y Open, parametro Open (Trigger), y transicion Closed->Open con condicion If Open, hasExitTime=false, defaultState=Closed.
    - Existe Assets/_Project/Animations/DoorOpen.anim que rota localEulerAngles.y de 0 a ~95 grados en ~0.5s.
    - El estado Open del controller referencia el clip DoorOpen (open.motion).
    - El generador vive bajo Editor/ y no es referenciado desde runtime.
    - Consola de Unity sin errores de compilacion.
  </acceptance_criteria>
  <done>Los assets Door.controller (2 estados + transicion condicional) y DoorOpen.anim existen y son inspeccionables en el Animator window; el generador Editor-only no rompe el build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguno) | Juego single-player offline de escritorio: sin red, sin cuentas, sin PII, sin entrada no confiable. Generacion de assets en tiempo de editor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | (n/a) | DoorAnimatorBuilder / assets de Animator | accept | Juego offline single-player; sin red/auth/PII/entrada no confiable. No hay amenazas aplicables a ASVS L1. |

> Sin instalaciones de paquetes (todo built-in de Unity 6 / URP) => no aplica Package Legitimacy Audit ni checkpoint T-04-SC.
</threat_model>

<verification>
- Compilacion limpia del script Editor (read_console sin errores; recordar que UnityEditor.* no debe filtrarse a runtime).
- Tras correr "Drunk Simulator -> Build Door Animator": Door.controller y DoorOpen.anim existen en Assets/_Project/Animations.
- Animator window: 2 estados + transicion condicional por parametro Open.
</verification>

<success_criteria>
- Los assets prerequisito de ANIM-01 existen y son correctos (>=2 estados + transicion condicional), listos para que el Plan 03 los enganche a un GameObject puerta.
</success_criteria>

<output>
Crear `.planning/phases/04-animaciones/04-02-SUMMARY.md` al completar.
</output>
